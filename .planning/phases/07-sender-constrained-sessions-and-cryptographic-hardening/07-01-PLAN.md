---
phase: 07-sender-constrained-sessions-and-cryptographic-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/models/session.py
  - backend/app/db/repositories/sessions.py
  - backend/app/models/evidence.py
  - backend/alembic/versions/0006_sender_constrained_replay_foundations.py
  - backend/tests/integration/auth/test_session_flow.py
  - backend/tests/security/test_search_capability_token.py
autonomous: true
requirements:
  - AUTH-12
  - AGNT-08
  - OBS-08
must_haves:
  truths:
    - "Replay-protected security artifacts have bounded persistent state instead of relying only on in-memory checks."
    - "Backend config exposes explicit feature flags and TTL controls for sender-constrained session, OAuth anti-replay, and capability anti-replay rollout."
    - "Replay or key-mismatch events have a reviewed persistence and evidence path before higher-level auth hardening layers are added."
  artifacts:
    - path: backend/alembic/versions/0006_sender_constrained_replay_foundations.py
      provides: database support for replay-journal and sender-constrained session metadata
      contains: replay
    - path: backend/app/models/session.py
      provides: sender-constrained session family metadata and replay-journal model fields
      min_lines: 40
    - path: backend/app/core/config.py
      provides: Phase 07 feature flags and cryptographic hardening TTL settings
      contains: dpop_
    - path: backend/app/db/repositories/sessions.py
      provides: typed replay-journal persistence helpers and evidence integration points
      min_lines: 40
  key_links:
    - from: backend/app/core/config.py
      to: backend/app/db/repositories/sessions.py
      via: replay-journal and DPoP-related TTL/config values
      pattern: dpop_|capability_replay|oauth_pkce
    - from: backend/app/models/session.py
      to: backend/alembic/versions/0006_sender_constrained_replay_foundations.py
      via: schema and migration agreement for new hardening fields
      pattern: key_thumbprint|replay
    - from: backend/app/db/repositories/sessions.py
      to: backend/tests/integration/auth/test_session_flow.py
      via: persisted replay evidence and deny-path assertions
      pattern: replay|security_event
---

<objective>
Establish the shared persistence and configuration foundations for Phase 07 so PKCE, asymmetric capability hardening, and DPoP-style sender-constrained sessions all land on one reviewed replay/evidence model instead of three disconnected ad-hoc implementations.

Purpose: create the database, repository, and config primitives that every later hardening plan depends on.
Output: replay-journal storage, sender-constrained session metadata fields, rollout flags, and failing-then-passing baseline tests for replay-aware persistence.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-CONTEXT.md
@backend/app/core/config.py
@backend/app/models/session.py
@backend/app/db/repositories/sessions.py
@backend/tests/integration/auth/test_session_flow.py
@backend/tests/security/test_search_capability_token.py
</context>

## Artifacts this phase produces

- Alembic revision `0006_sender_constrained_replay_foundations.py` for replay-journal and sender-constrained session metadata.
- New config keys for DPoP, OAuth PKCE, and capability anti-replay rollout.
- New persistence helpers in `SessionsRepository` for consuming one-time security artifacts and recording replay evidence.
- Extended session models for key-thumbprint / binding metadata and shared replay records.

<tasks>
<task type="auto">
  <name>Task 1: Lock the replay-foundation contract in baseline security tests</name>
  <files>backend/tests/integration/auth/test_session_flow.py, backend/tests/security/test_search_capability_token.py</files>
  <read_first>backend/tests/integration/auth/test_session_flow.py, backend/tests/security/test_search_capability_token.py, backend/app/models/session.py, backend/app/db/repositories/sessions.py</read_first>
  <action>Add failing baseline tests that require persistent replay tracking and sender-constrained metadata before later auth plans can use them. Extend `test_session_flow.py` so replay-oriented flows can assert stored family binding metadata and correlated security evidence hooks, and extend `test_search_capability_token.py` so a one-time capability credential can be rejected on second use through a persistence-backed consume path rather than only JWT validation.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_search_capability_token.py -x</verify>
  <acceptance_criteria>
    - `backend/tests/integration/auth/test_session_flow.py` contains assertions about sender-constrained metadata or replay persistence rather than cookie-only state.
    - `backend/tests/security/test_search_capability_token.py` contains a replay-denial path that expects second-use failure.
    - The targeted pytest command fails before implementation because the replay foundation does not yet exist.
  </acceptance_criteria>
  <done>The tests express the new replay-journal and binding expectations before repository/model changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Add replay-journal schema and sender-constrained session metadata</name>
  <files>backend/app/core/config.py, backend/app/models/session.py, backend/alembic/versions/0006_sender_constrained_replay_foundations.py</files>
  <read_first>backend/app/core/config.py, backend/app/models/session.py, backend/alembic/versions/0005_websearch_provider_runtime_override.py, backend/alembic/versions/0003_python_execution_contracts.py</read_first>
  <action>Add concrete Phase 07 config keys in `backend/app/core/config.py`: `dpop_enabled`, `dpop_nonce_ttl_seconds`, `dpop_proof_leeway_seconds`, `oauth_pkce_enabled`, `capability_replay_protection_enabled`, and any shared replay-journal TTL needed by later plans. Extend `backend/app/models/session.py` with exact sender-constrained family metadata such as `key_thumbprint` and `auth_binding_method`, plus a shared replay-journal model/table for one-time security artifact `jti` consumption. Create `backend/alembic/versions/0006_sender_constrained_replay_foundations.py` so the schema exactly matches the new session and replay structures.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_search_capability_token.py -x</verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` contains the exact keys `dpop_enabled`, `oauth_pkce_enabled`, and `capability_replay_protection_enabled`.
    - `backend/app/models/session.py` contains sender-constrained family fields such as `key_thumbprint` or `auth_binding_method` plus a replay-record model.
    - `backend/alembic/versions/0006_sender_constrained_replay_foundations.py` exists and creates matching columns/tables for the new model state.
  </acceptance_criteria>
  <done>The codebase has a reviewed schema and config foundation for all later Phase 07 hardening work.</done>
</task>

<task type="auto">
  <name>Task 3: Add repository consume-once helpers and evidence-ready replay persistence</name>
  <files>backend/app/db/repositories/sessions.py, backend/app/models/evidence.py, backend/tests/integration/auth/test_session_flow.py, backend/tests/security/test_search_capability_token.py</files>
  <read_first>backend/app/db/repositories/sessions.py, backend/app/models/evidence.py, backend/app/models/session.py, backend/tests/integration/auth/test_session_flow.py, backend/tests/security/test_search_capability_token.py</read_first>
  <action>Extend `SessionsRepository` with typed helpers to persist, check, and consume one-time security artifact identifiers and to attach correlated security evidence on replay or key-mismatch paths. Reuse the existing security-event idiom rather than inventing a separate logging sink. If `backend/app/models/evidence.py` needs enumerated or structural support for the new replay event types, add only the exact fields/constants necessary for DPoP, OAuth transaction, and capability replay evidence to share one reviewed pathway.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_search_capability_token.py -x</verify>
  <acceptance_criteria>
    - `backend/app/db/repositories/sessions.py` contains helper methods for replay record lookup/insert/consume rather than ad-hoc inline SQL in services.
    - The repository still exposes correlated security-event recording through the existing evidence model.
    - The targeted pytest command passes after the repository/model/config work is complete.
  </acceptance_criteria>
  <done>Later plans can consume one-time proofs or capability tokens through one shared reviewed repository interface.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_search_capability_token.py -x`
- [ ] New config keys, session model fields, and migration names are grep-verifiable in the expected files.
- [ ] Replay persistence paths create or prepare correlated evidence hooks instead of silent deny-only behavior.
</verification>

<success_criteria>
- All tasks completed.
- Shared replay persistence exists before OAuth, capability, or DPoP-specific work lands.
- Sender-constrained session metadata has schema support.
- No new hardening flow in later plans will need to invent its own replay table or config flag set.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-01-SUMMARY.md`
</output>
