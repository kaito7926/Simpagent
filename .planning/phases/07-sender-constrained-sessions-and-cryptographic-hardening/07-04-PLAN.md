---
phase: 07-sender-constrained-sessions-and-cryptographic-hardening
plan: "04"
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - backend/app/security/access_tokens.py
  - backend/app/security/dpop.py
  - backend/app/services/authentication.py
  - backend/app/services/sessions.py
  - backend/app/api/routes/auth.py
  - backend/app/authorization/principal.py
  - backend/tests/integration/auth/test_session_flow.py
  - backend/tests/security/test_jwt_profile.py
autonomous: true
requirements:
  - AUTH-11
  - AUTH-12
  - OBS-08
must_haves:
  truths:
    - "A copied access token or refresh cookie without the bound client proof is denied on hardened protected paths."
    - "Access tokens and refresh families are cryptographically bound to a client key thumbprint rather than acting as bearer-only credentials."
    - "DPoP proof replay, nonce abuse, or key-binding mismatch is fail-closed and creates correlated evidence."
  artifacts:
    - path: backend/app/security/dpop.py
      provides: DPoP proof parsing, thumbprint derivation, and replay/nonce validation
      min_lines: 60
    - path: backend/app/security/access_tokens.py
      provides: sender-constrained access-token claim support
      contains: cnf
    - path: backend/app/api/routes/auth.py
      provides: auth route enforcement for bound client proofs
      min_lines: 80
  key_links:
    - from: backend/app/security/dpop.py
      to: backend/app/api/routes/auth.py
      via: request proof validation on login, refresh, logout, and protected session surfaces
      pattern: DPoP|dpop
    - from: backend/app/security/access_tokens.py
      to: backend/app/authorization/principal.py
      via: `cnf` key-thumbprint claim issuance and verification
      pattern: cnf|jkt
    - from: backend/app/services/sessions.py
      to: backend/tests/integration/auth/test_session_flow.py
      via: refresh-family binding and replay/key-mismatch denial evidence
      pattern: thumbprint|replay|nonce
---

<objective>
Bind SimpAgent's backend auth session lifecycle to DPoP-style sender-constrained proofs so copied browser session artifacts cannot be replayed successfully without the reviewed client key.

Purpose: deliver the core anti-`web2api` hardening layer for auth and protected request surfaces after the replay foundation and OAuth hardening are in place.
Output: DPoP proof helper module, sender-constrained access-token claims, refresh-family client binding, route enforcement, and backend auth regression tests.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-CONTEXT.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-01-PLAN.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-02-PLAN.md
@backend/app/security/access_tokens.py
@backend/app/services/authentication.py
@backend/app/services/sessions.py
@backend/app/api/routes/auth.py
@backend/app/authorization/principal.py
@backend/tests/integration/auth/test_session_flow.py
@backend/tests/security/test_jwt_profile.py
</context>

## Artifacts this phase produces

- New `backend/app/security/dpop.py` helper module for proof parsing, JWK thumbprint derivation, nonce validation, and replay checks.
- Access-token `cnf` binding support and principal-side claim verification.
- Refresh-family client-binding logic for login, refresh, and logout.
- Auth integration/security tests proving copied token/cookie replay is denied without proof material.

<tasks>
<task type="auto">
  <name>Task 1: Lock sender-constrained auth behavior in integration and security tests</name>
  <files>backend/tests/integration/auth/test_session_flow.py, backend/tests/security/test_jwt_profile.py</files>
  <read_first>backend/tests/integration/auth/test_session_flow.py, backend/tests/security/test_jwt_profile.py, backend/app/api/routes/auth.py, backend/app/security/access_tokens.py</read_first>
  <action>Add failing tests for the exact Phase 07 auth hardening contract: copied access tokens without a valid bound proof are rejected, refresh flows deny key mismatch or proof replay, and sender-constrained claim validation happens in backend authority rather than at the gateway. Keep the tests grounded in the actual login/refresh/logout/protected-session routes and the strict JWT profile already verified in Phase 01/05.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_jwt_profile.py -x</verify>
  <acceptance_criteria>
    - `backend/tests/integration/auth/test_session_flow.py` contains explicit sender-constrained replay or key-mismatch deny-path assertions.
    - `backend/tests/security/test_jwt_profile.py` contains `cnf` or DPoP-related backend validation expectations.
    - The targeted pytest command fails before implementation because the auth stack is still bearer-style.
  </acceptance_criteria>
  <done>The tests define the DPoP sender-constrained auth contract before service/route changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Add DPoP proof helpers and sender-constrained access-token claims</name>
  <files>backend/app/security/access_tokens.py, backend/app/security/dpop.py, backend/app/authorization/principal.py</files>
  <read_first>backend/app/security/access_tokens.py, backend/app/authorization/principal.py, backend/app/security/search_capability.py, backend/app/security/csrf.py</read_first>
  <action>Create `backend/app/security/dpop.py` with exact helpers for JWK thumbprint derivation, DPoP proof parsing, nonce and `jti` replay validation, and method/URI binding checks. Extend `backend/app/security/access_tokens.py` so access tokens can carry a concrete `cnf` claim containing the bound key thumbprint, and update `backend/app/authorization/principal.py` to reject protected access when the incoming proof-bound key does not match the token claim under the hardening flag.</action>
  <verify>python -m pytest -q backend/tests/security/test_jwt_profile.py -x</verify>
  <acceptance_criteria>
    - `backend/app/security/dpop.py` exists and contains thumbprint, proof, and nonce/replay validation helpers.
    - `backend/app/security/access_tokens.py` contains `cnf` claim support and corresponding decode validation.
    - `backend/app/authorization/principal.py` verifies the sender-constrained binding instead of treating bearer JWT validation alone as sufficient.
  </acceptance_criteria>
  <done>The backend has reusable proof-validation and token-binding primitives for sender-constrained auth.</done>
</task>

<task type="auto">
  <name>Task 3: Bind login/refresh/logout flows to client proof material and evidence</name>
  <files>backend/app/services/authentication.py, backend/app/services/sessions.py, backend/app/api/routes/auth.py, backend/tests/integration/auth/test_session_flow.py</files>
  <read_first>backend/app/services/authentication.py, backend/app/services/sessions.py, backend/app/api/routes/auth.py, backend/app/security/dpop.py, backend/tests/integration/auth/test_session_flow.py</read_first>
  <action>Update login, refresh, and logout to issue and validate sender-constrained session state. On login, bind the refresh family and access token to the validated client key thumbprint. On refresh and logout, require a matching DPoP-style proof and fail closed on missing proof, key mismatch, nonce abuse, or replay, recording correlated evidence through the existing security-event path. Keep the existing CSRF and Origin protections in place; DPoP supplements them rather than replacing them. Ensure auth route behavior stays truthful and does not leak raw proof contents.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_jwt_profile.py -x</verify>
  <acceptance_criteria>
    - `backend/app/services/authentication.py` binds a concrete key thumbprint to the issued session family and access token.
    - `backend/app/services/sessions.py` denies refresh or logout on proof replay or key mismatch and records correlated evidence.
    - `backend/app/api/routes/auth.py` extracts or requires the reviewed DPoP-style proof path for the hardened routes.
    - The targeted pytest command passes, proving the auth stack is sender-constrained rather than bearer-only.
  </acceptance_criteria>
  <done>Core backend auth routes reject copied browser session artifacts unless the bound client proof is present and valid.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q backend/tests/integration/auth/test_session_flow.py backend/tests/security/test_jwt_profile.py -x`
- [ ] `backend/app/security/access_tokens.py` and `backend/app/authorization/principal.py` both enforce `cnf`/thumbprint semantics.
- [ ] Refresh/logout deny paths still preserve CSRF/Origin enforcement while adding proof validation and evidence.
</verification>

<success_criteria>
- All tasks completed.
- Access and refresh session artifacts are bound to client proof material.
- Replay or mismatch is fail-closed with evidence.
- Backend remains the authorization authority for sender-constrained validation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-04-SUMMARY.md`
</output>
