---
phase: 07-sender-constrained-sessions-and-cryptographic-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/app/api/routes/auth_oauth.py
  - backend/app/identity/providers/google.py
  - backend/app/identity/providers/github.py
  - backend/app/security/oauth_transaction.py
  - backend/tests/integration/auth/test_oauth_flows.py
  - backend/tests/unit/test_oauth_state_cookie.py
  - backend/tests/integration/auth/test_google_oauth.py
  - backend/tests/integration/auth/test_github_oauth.py
autonomous: true
requirements:
  - IDEN-09
  - OBS-08
must_haves:
  truths:
    - "OAuth authorization-code callbacks cannot complete with replayed, swapped, or provider-mismatched transaction artifacts."
    - "Google and GitHub OAuth starts send PKCE S256 challenge material and callbacks validate one-time sealed transaction state before exchanging codes."
    - "Replay or transaction mismatch creates correlated denial evidence instead of a silent callback failure."
  artifacts:
    - path: backend/app/security/oauth_transaction.py
      provides: sealed OAuth transaction issue/verify helpers with one-time replay semantics
      min_lines: 40
    - path: backend/app/api/routes/auth_oauth.py
      provides: PKCE-enabled start and callback flow for Google and GitHub
      contains: code_challenge
    - path: backend/tests/integration/auth/test_oauth_flows.py
      provides: provider-agnostic anti-replay and transaction validation coverage
      min_lines: 30
  key_links:
    - from: backend/app/api/routes/auth_oauth.py
      to: backend/app/security/oauth_transaction.py
      via: sealed transaction issue and callback verification
      pattern: oauth_transaction|code_challenge|code_verifier
    - from: backend/app/identity/providers/google.py
      to: backend/app/api/routes/auth_oauth.py
      via: PKCE authorization request parameters
      pattern: code_challenge
    - from: backend/tests/integration/auth/test_oauth_flows.py
      to: backend/app/api/routes/auth_oauth.py
      via: callback replay, provider mismatch, and PKCE failure assertions
      pattern: oauth_.*invalid|oauth_.*failed
---

<objective>
Add PKCE S256 and sealed one-time transaction hardening to SimpAgent's backend-owned Google and GitHub OAuth flows so callback artifacts cannot be replayed or swapped across providers.

Purpose: close the biggest OAuth browser-flow gap identified in the hardening research without changing the overall backend-owned redirect model introduced in Phase 05.
Output: a sealed OAuth transaction helper, PKCE-enabled provider starts, callback replay protection, and updated integration/unit tests for Google and GitHub OAuth security.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-CONTEXT.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-01-PLAN.md
@backend/app/api/routes/auth_oauth.py
@backend/app/identity/providers/google.py
@backend/app/identity/providers/github.py
@backend/tests/integration/auth/test_oauth_flows.py
@backend/tests/unit/test_oauth_state_cookie.py
</context>

## Artifacts this phase produces

- New `backend/app/security/oauth_transaction.py` helper for sealed one-time OAuth transaction handling.
- PKCE S256 parameters in Google and GitHub OAuth start flows.
- Callback verification logic for one-time transaction use, provider binding, and PKCE exchange support.
- Expanded OAuth integration tests covering replay, mismatch, and fail-closed callback paths.

<tasks>
<task type="auto">
  <name>Task 1: Lock PKCE and one-time transaction behavior in OAuth tests</name>
  <files>backend/tests/integration/auth/test_oauth_flows.py, backend/tests/unit/test_oauth_state_cookie.py, backend/tests/integration/auth/test_google_oauth.py, backend/tests/integration/auth/test_github_oauth.py</files>
  <read_first>backend/tests/integration/auth/test_oauth_flows.py, backend/tests/unit/test_oauth_state_cookie.py, backend/tests/integration/auth/test_google_oauth.py, backend/tests/integration/auth/test_github_oauth.py, backend/app/api/routes/auth_oauth.py</read_first>
  <action>Add failing tests that assert the exact new Phase 07 behavior: start routes emit PKCE challenge material, callback flows fail closed when transaction records are replayed or bound to the wrong provider, and callback completion depends on a one-time sealed transaction rather than signed `state` alone. Keep the test contract backend-owned and use both provider-specific and provider-agnostic suites where they already exist.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_oauth_flows.py backend/tests/integration/auth/test_google_oauth.py backend/tests/integration/auth/test_github_oauth.py backend/tests/unit/test_oauth_state_cookie.py -x</verify>
  <acceptance_criteria>
    - `backend/tests/integration/auth/test_oauth_flows.py` contains explicit replay or provider-mismatch callback assertions.
    - `backend/tests/unit/test_oauth_state_cookie.py` evolves beyond SameSite-only checks to cover sealed transaction or PKCE-related cookie invariants where appropriate.
    - The targeted pytest command fails before implementation because PKCE/transaction hardening is not present yet.
  </acceptance_criteria>
  <done>The tests clearly define the anti-replay and PKCE contract before route/provider code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Add sealed OAuth transaction helpers and provider PKCE support</name>
  <files>backend/app/security/oauth_transaction.py, backend/app/identity/providers/google.py, backend/app/identity/providers/github.py</files>
  <read_first>backend/app/identity/providers/google.py, backend/app/identity/providers/github.py, backend/app/api/routes/auth_oauth.py, backend/app/security/csrf.py</read_first>
  <action>Create `backend/app/security/oauth_transaction.py` with exact helpers to issue, verify, and mark one-time OAuth transactions as consumed. Include provider binding, `state`, PKCE verifier or verifier-handle support, issuance time, and replay-denial hooks that can connect to the replay foundation from Plan 07-01. Update the Google and GitHub provider request builders so authorization starts include `code_challenge_method=S256` and the exact PKCE challenge parameter names required by those providers.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_oauth_flows.py backend/tests/integration/auth/test_google_oauth.py backend/tests/integration/auth/test_github_oauth.py backend/tests/unit/test_oauth_state_cookie.py -x</verify>
  <acceptance_criteria>
    - `backend/app/security/oauth_transaction.py` exists and contains issue/verify/consume-style helpers for provider-bound transactions.
    - `backend/app/identity/providers/google.py` and `backend/app/identity/providers/github.py` both contain `code_challenge` logic and S256 challenge method handling.
    - The helper design reuses the replay foundation from Plan 07-01 instead of inventing isolated in-memory callback tracking.
  </acceptance_criteria>
  <done>The codebase has the reusable PKCE/transaction building blocks needed by both OAuth providers.</done>
</task>

<task type="auto">
  <name>Task 3: Wire PKCE and one-time transaction validation into OAuth routes and evidence</name>
  <files>backend/app/api/routes/auth_oauth.py, backend/tests/integration/auth/test_oauth_flows.py, backend/tests/integration/auth/test_google_oauth.py, backend/tests/integration/auth/test_github_oauth.py</files>
  <read_first>backend/app/api/routes/auth_oauth.py, backend/app/security/oauth_transaction.py, backend/app/identity/oauth_service.py, backend/tests/integration/auth/test_oauth_flows.py</read_first>
  <action>Update `backend/app/api/routes/auth_oauth.py` so `/start` routes issue sealed provider-bound PKCE transactions and `/callback` routes verify and consume them before exchanging the code. Keep the existing truthful unconfigured-provider behavior and backend-owned session issuance model. Add correlated denial evidence for replay, provider mismatch, or malformed transaction failures using the same evidence idiom already used for other security events. Preserve the existing user-facing fail-closed API error shape rather than leaking transaction internals.</action>
  <verify>python -m pytest -q backend/tests/integration/auth/test_oauth_flows.py backend/tests/integration/auth/test_google_oauth.py backend/tests/integration/auth/test_github_oauth.py backend/tests/unit/test_oauth_state_cookie.py -x</verify>
  <acceptance_criteria>
    - `backend/app/api/routes/auth_oauth.py` contains `code_challenge` issuance and callback-side verifier/transaction validation before provider authenticate/exchange calls.
    - Replay or provider mismatch paths return fail-closed OAuth errors without exposing secrets or raw verifier material.
    - The targeted pytest command passes for Google, GitHub, and shared OAuth anti-replay coverage.
  </acceptance_criteria>
  <done>OAuth callback artifacts are PKCE-bound, one-time, and provider-specific rather than reusable signed-state bearer material.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q backend/tests/integration/auth/test_oauth_flows.py backend/tests/integration/auth/test_google_oauth.py backend/tests/integration/auth/test_github_oauth.py backend/tests/unit/test_oauth_state_cookie.py -x`
- [ ] `backend/app/api/routes/auth_oauth.py` contains PKCE challenge issuance and transaction verification before code exchange.
- [ ] Replay/mismatch denial paths use safe errors and evidence hooks instead of silent fallback.
</verification>

<success_criteria>
- All tasks completed.
- Both Google and GitHub OAuth flows gain PKCE S256 and one-time transaction validation.
- Callback replay or provider-swap attacks fail closed with evidence.
- Existing truthful provider-unconfigured behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-02-SUMMARY.md`
</output>
