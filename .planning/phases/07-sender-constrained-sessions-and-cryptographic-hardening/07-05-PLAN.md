---
phase: 07-sender-constrained-sessions-and-cryptographic-hardening
plan: "05"
type: execute
wave: 4
depends_on:
  - 07-03
  - 07-04
files_modified:
  - frontend/lib/device-proof.ts
  - frontend/lib/auth-session.ts
  - frontend/components/account-access/AccountAccessShell.tsx
  - frontend/tests/auth-session.test.ts
  - frontend/tests/account-access-oauth.test.tsx
  - docs/security.vi.md
  - docs/runbook.vi.md
  - docs/limitations.vi.md
autonomous: true
requirements:
  - AUTH-11
  - AUTH-12
  - PRODREADY-06
  - OBS-08
must_haves:
  truths:
    - "The browser auth shell generates and uses reviewed device proof material for hardened session routes without falling back to browser storage of long-lived bearer secrets."
    - "Frontend auth recovery stays truthful when device proof material is lost or mismatched; the user is forced to re-authenticate instead of silently degrading to bearer mode."
    - "Vietnamese security/runbook/limitations documentation explains sender-constrained rollout behavior, re-auth expectations, and residual prototype limits."
  artifacts:
    - path: frontend/lib/device-proof.ts
      provides: browser-held device proof bootstrap and request-proof helpers
      min_lines: 40
    - path: frontend/lib/auth-session.ts
      provides: DPoP-style proof integration for login, refresh, logout, and protected requests
      min_lines: 80
    - path: docs/runbook.vi.md
      provides: operator guidance for key loss, replay evidence, and sender-constrained rollout
      contains: DPoP
  key_links:
    - from: frontend/lib/device-proof.ts
      to: frontend/lib/auth-session.ts
      via: auth/session controller uses browser-held proof helpers for protected requests
      pattern: proof|dpop|thumbprint
    - from: frontend/lib/auth-session.ts
      to: frontend/tests/auth-session.test.ts
      via: session and refresh behavior regression coverage for proof-bound requests
      pattern: refresh|Authorization|DPoP
    - from: docs/security.vi.md
      to: docs/runbook.vi.md
      via: truthful user/operator explanation of sender-constrained behavior and limits
      pattern: DPoP|PKCE|re-auth
---

<objective>
Integrate the sender-constrained session model into the browser auth shell and update the Vietnamese operational/security documentation so Phase 07 closes with truthful UX, rollout guidance, and regression coverage.

Purpose: finish the MVP hardening slice by connecting backend proof requirements to the real frontend session controller and by documenting how the new cryptographic trust model behaves in practice.
Output: browser-held device proof helper, frontend auth-session integration, auth-shell regression coverage, and Vietnamese security/runbook/limitations updates.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-CONTEXT.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-03-PLAN.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-04-PLAN.md
@frontend/lib/auth-session.ts
@frontend/components/account-access/AccountAccessShell.tsx
@frontend/tests/auth-session.test.ts
@frontend/tests/account-access-oauth.test.tsx
@docs/security.vi.md
@docs/runbook.vi.md
@docs/limitations.vi.md
</context>

## Artifacts this phase produces

- New `frontend/lib/device-proof.ts` helper for browser-held proof bootstrap and request-signing support.
- Updated auth-session controller to attach reviewed proof material to login, refresh, logout, and protected requests.
- Frontend regression tests for proof-bound refresh behavior and OAuth/session UX under device-key mismatch or loss.
- Updated Vietnamese security, runbook, and limitations docs covering sender-constrained rollout and re-auth expectations.

<tasks>
<task type="auto">
  <name>Task 1: Lock proof-bound frontend session behavior in auth-shell tests</name>
  <files>frontend/tests/auth-session.test.ts, frontend/tests/account-access-oauth.test.tsx</files>
  <read_first>frontend/tests/auth-session.test.ts, frontend/tests/account-access-oauth.test.tsx, frontend/lib/auth-session.ts, frontend/components/account-access/AccountAccessShell.tsx</read_first>
  <action>Add failing frontend tests for the Phase 07 UX contract: protected requests attach reviewed proof material, refresh/login/logout cannot silently fall back to bearer-only mode, and key-loss or proof-mismatch paths move the shell into a truthful re-auth/session-expired state. Extend the OAuth auth-shell tests where necessary so the browser bootstrap path remains compatible with backend-owned OAuth starts after PKCE hardening.</action>
  <verify>npm test -- --runInBand tests/auth-session.test.ts tests/account-access-oauth.test.tsx</verify>
  <acceptance_criteria>
    - `frontend/tests/auth-session.test.ts` contains assertions about proof-bound request behavior or re-auth on proof failure.
    - `frontend/tests/account-access-oauth.test.tsx` covers the auth-shell/OAuth bootstrap contract under the new hardening model.
    - The targeted frontend test command fails before implementation because device-proof integration does not yet exist.
  </acceptance_criteria>
  <done>The frontend regression suite defines the sender-constrained auth-shell behavior before implementation lands.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser-held device proof helpers and wire the auth session controller</name>
  <files>frontend/lib/device-proof.ts, frontend/lib/auth-session.ts, frontend/components/account-access/AccountAccessShell.tsx, frontend/tests/auth-session.test.ts, frontend/tests/account-access-oauth.test.tsx</files>
  <read_first>frontend/lib/auth-session.ts, frontend/components/account-access/AccountAccessShell.tsx, frontend/tests/auth-session.test.ts, frontend/tests/account-access-oauth.test.tsx</read_first>
  <action>Create `frontend/lib/device-proof.ts` with the exact browser-side helper responsibilities required by the backend DPoP-style contract: bootstrap a browser-held key pair or proof handle, derive or expose the bound key thumbprint, and generate per-request proof headers without persisting long-lived bearer secrets in localStorage. Update `frontend/lib/auth-session.ts` so login, refresh, logout, and protected requests obtain proof material from that helper, and update `AccountAccessShell.tsx` so bootstrap, error handling, and session-expired UX remain truthful when proof material is missing or mismatched.</action>
  <verify>npm test -- --runInBand tests/auth-session.test.ts tests/account-access-oauth.test.tsx</verify>
  <acceptance_criteria>
    - `frontend/lib/device-proof.ts` exists and centralizes proof bootstrap/signing behavior.
    - `frontend/lib/auth-session.ts` references the proof helper on login, refresh, logout, and protected request paths.
    - `AccountAccessShell.tsx` handles proof bootstrap or proof-loss failure paths without suggesting bearer fallback.
    - The targeted frontend test command passes after implementation.
  </acceptance_criteria>
  <done>The browser auth shell actively participates in the sender-constrained trust model instead of assuming bearer semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Update Vietnamese security, runbook, and limitation docs for the hardened trust model</name>
  <files>docs/security.vi.md, docs/runbook.vi.md, docs/limitations.vi.md</files>
  <read_first>docs/security.vi.md, docs/runbook.vi.md, docs/limitations.vi.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-CONTEXT.md</read_first>
  <action>Update the Vietnamese documentation set so it truthfully explains the new sender-constrained behavior. In `docs/security.vi.md`, document PKCE + sealed transaction, DPoP-style proof-bound sessions, and asymmetric consume-once capability boundaries. In `docs/runbook.vi.md`, add operator guidance for replay alerts, key mismatch, local browser key loss, and the required re-auth or session reset path. In `docs/limitations.vi.md`, state the remaining prototype limits clearly: WebAuthn step-up is still deferred, browser proof material loss forces re-auth, and the hardening scope covers the reviewed Phase 07 surfaces rather than every hypothetical future endpoint.</action>
  <verify>rg -n "DPoP|PKCE|sender-constrained|proof|re-auth|WebAuthn" docs/security.vi.md docs/runbook.vi.md docs/limitations.vi.md</verify>
  <acceptance_criteria>
    - `docs/security.vi.md` mentions DPoP-style or sender-constrained sessions plus PKCE hardening.
    - `docs/runbook.vi.md` contains replay/key-loss/re-auth guidance.
    - `docs/limitations.vi.md` explicitly states WebAuthn remains deferred and proof-material loss forces re-auth.
  </acceptance_criteria>
  <done>The operator-facing docs match the actual Phase 07 hardening behavior and limitations.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --runInBand tests/auth-session.test.ts tests/account-access-oauth.test.tsx`
- [ ] `rg -n "DPoP|PKCE|sender-constrained|proof|re-auth|WebAuthn" docs/security.vi.md docs/runbook.vi.md docs/limitations.vi.md`
- [ ] Frontend auth-shell behavior remains truthful under proof loss or mismatch and does not store long-lived bearer credentials in browser storage.
</verification>

<success_criteria>
- All tasks completed.
- Frontend auth/session behavior matches the backend sender-constrained contract.
- Users are forced through truthful re-auth when proof material is lost or mismatched.
- Vietnamese documentation accurately explains rollout, operations, and residual limits.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-05-SUMMARY.md`
</output>
