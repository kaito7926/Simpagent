---
phase: 07-sender-constrained-sessions-and-cryptographic-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/app/security/search_capability.py
  - backend/app/security/tool_capabilities.py
  - backend/app/ai/search_worker/service.py
  - backend/app/tools/python_client.py
  - sandbox/server.py
  - backend/tests/security/test_search_capability_token.py
  - backend/tests/security/test_python_request_hardening.py
autonomous: true
requirements:
  - AGNT-08
  - AGNT-09
  - OBS-08
must_haves:
  truths:
    - "Search and Python capability credentials are asymmetrically signed, audience-bound, and rejected on replay."
    - "The search worker and Python supervisor do not accept old reusable symmetric bearer semantics once Phase 07 hardening is enabled."
    - "Capability replay or mismatch failures create safe correlated evidence without exposing raw proof material."
  artifacts:
    - path: backend/app/security/search_capability.py
      provides: replay-aware asymmetric search capability issuance and validation
      contains: jti
    - path: backend/app/security/tool_capabilities.py
      provides: asymmetric Python capability issuance bound to reviewed request context
      min_lines: 40
    - path: sandbox/server.py
      provides: public-key verification and one-time consume behavior for Python supervisor capabilities
      min_lines: 80
  key_links:
    - from: backend/app/security/search_capability.py
      to: backend/app/ai/search_worker/service.py
      via: capability validation and consume-once enforcement in worker execution
      pattern: validate_search_capability|consume
    - from: backend/app/security/tool_capabilities.py
      to: backend/app/tools/python_client.py
      via: capability token shape and invocation contract
      pattern: capability
    - from: backend/app/tools/python_client.py
      to: sandbox/server.py
      via: reviewed capability payload exchanged with the Python supervisor
      pattern: capability
---

<objective>
Convert SimpAgent's search and Python capability boundaries from reusable short-lived bearer semantics into asymmetric, audience-bound, consume-once trust artifacts backed by the shared replay foundation.

Purpose: harden the internal worker boundaries before browser-side sender-constrained sessions are introduced, reducing the chance that copied worker artifacts can be reused as unofficial API calls.
Output: updated search and Python capability issuance/validation, replay denial, and worker/supervisor verification tests.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-CONTEXT.md
@.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-01-PLAN.md
@backend/app/security/search_capability.py
@backend/app/security/tool_capabilities.py
@backend/app/ai/search_worker/service.py
@backend/app/tools/python_client.py
@sandbox/server.py
@backend/tests/security/test_search_capability_token.py
@backend/tests/security/test_python_request_hardening.py
</context>

## Artifacts this phase produces

- Replay-aware asymmetric search capability issuance and validation.
- Asymmetric Python capability issuance compatible with the reviewed Python supervisor boundary.
- Updated worker/supervisor verification paths that consume capability `jti` exactly once.
- Security tests proving replay denial and fail-closed behavior for both search and Python capability boundaries.

<tasks>
<task type="auto">
  <name>Task 1: Lock search and Python capability replay behavior in security tests</name>
  <files>backend/tests/security/test_search_capability_token.py, backend/tests/security/test_python_request_hardening.py</files>
  <read_first>backend/tests/security/test_search_capability_token.py, backend/tests/security/test_python_request_hardening.py, backend/app/security/search_capability.py, backend/app/security/tool_capabilities.py, sandbox/server.py</read_first>
  <action>Add failing tests for the exact Phase 07 contract: the same search capability or Python capability cannot be consumed twice, audience/request-context mismatches fail closed, and the hardened boundaries do not silently accept the previous reusable symmetric/bearer semantics. Keep the tests grounded in the real worker/supervisor contract rather than only checking token helper functions in isolation.</action>
  <verify>python -m pytest -q backend/tests/security/test_search_capability_token.py backend/tests/security/test_python_request_hardening.py -x</verify>
  <acceptance_criteria>
    - `backend/tests/security/test_search_capability_token.py` contains a consume-once or replay-denial assertion.
    - `backend/tests/security/test_python_request_hardening.py` contains a supervisor capability replay or mismatch denial assertion.
    - The targeted pytest command fails before implementation because replay-aware asymmetric enforcement is not yet present.
  </acceptance_criteria>
  <done>The tests define the replay-resistant internal capability boundary before token or worker code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Harden search capability issuance and worker consume-once validation</name>
  <files>backend/app/security/search_capability.py, backend/app/ai/search_worker/service.py, backend/tests/security/test_search_capability_token.py</files>
  <read_first>backend/app/security/search_capability.py, backend/app/ai/search_worker/service.py, backend/tests/security/test_search_capability_token.py, backend/app/services/chat_turns.py</read_first>
  <action>Extend `backend/app/security/search_capability.py` so the search capability remains asymmetrically signed but now carries the exact replay- and context-binding metadata later code will enforce, including a one-time `jti` consumption path rooted in the replay foundation from Plan 07-01. Update `backend/app/ai/search_worker/service.py` to validate and consume the capability before live execution, deny second use, and record correlated safe evidence for replay or mismatch failures.</action>
  <verify>python -m pytest -q backend/tests/security/test_search_capability_token.py -x</verify>
  <acceptance_criteria>
    - `backend/app/security/search_capability.py` still uses asymmetric signing and now exposes replay-aware validation inputs or consume helpers.
    - `backend/app/ai/search_worker/service.py` consumes or rejects capability `jti` before executing search.
    - The search capability tests pass with replay denial rather than simple JWT-expiry-only logic.
  </acceptance_criteria>
  <done>The search worker accepts only reviewed, one-time asymmetric capability credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Convert Python capability verification to asymmetric consume-once semantics</name>
  <files>backend/app/security/tool_capabilities.py, backend/app/tools/python_client.py, sandbox/server.py, backend/tests/security/test_python_request_hardening.py</files>
  <read_first>backend/app/security/tool_capabilities.py, backend/app/tools/python_client.py, sandbox/server.py, backend/tests/security/test_python_request_hardening.py</read_first>
  <action>Replace the reviewed Python capability issuance path in `backend/app/security/tool_capabilities.py` with an asymmetric token format that keeps binding to `execution_id`, `profile_name`, `code_hash`, and `state_snapshot_hash`. Update `backend/app/tools/python_client.py` only as needed to transport the hardened token shape, and update `sandbox/server.py` so the supervisor verifies with a public-key trust path and consumes the capability `jti` exactly once. Remove any hard dependency on a shared signing secret for successful verification when the hardening flag is enabled, while keeping safe failure behavior and correlated evidence on replay or mismatch.</action>
  <verify>python -m pytest -q backend/tests/security/test_python_request_hardening.py -x</verify>
  <acceptance_criteria>
    - `backend/app/security/tool_capabilities.py` no longer relies solely on a shared HMAC secret for the hardened success path.
    - `sandbox/server.py` verifies a public-key-based capability path and rejects second-use capability tokens.
    - The targeted pytest command passes and proves replay denial for Python supervisor requests.
  </acceptance_criteria>
  <done>The Python supervisor boundary is asymmetric, audience-bound, and consume-once rather than reusable symmetric bearer material.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q backend/tests/security/test_search_capability_token.py backend/tests/security/test_python_request_hardening.py -x`
- [ ] Search and Python capability code both enforce consume-once semantics.
- [ ] No hardened success path relies only on reusable shared bearer semantics.
</verification>

<success_criteria>
- All tasks completed.
- Search and Python capabilities are asymmetric and replay-resistant.
- Worker and supervisor boundaries fail closed on mismatch or reuse.
- Evidence hooks remain safe and correlated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sender-constrained-sessions-and-cryptographic-hardening/07-03-SUMMARY.md`
</output>
