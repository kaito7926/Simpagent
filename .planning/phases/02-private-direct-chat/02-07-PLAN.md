---
phase: 02-private-direct-chat
plan: "07"
type: execute
wave: 7
depends_on:
  - 02-05
  - 02-06
files_modified:
  - backend/tests/smoke/test_private_direct_chat.py
  - backend/app/api/routes/chat.py
  - backend/app/services/chat.py
  - backend/app/db/repositories/conversations.py
  - backend/app/ai/chat_adapter.py
  - frontend/components/chat/ChatWorkspace.tsx
  - frontend/components/chat/ChatSidebar.tsx
  - frontend/components/chat/MessageMarkdown.tsx
  - frontend/lib/chat-api.ts
autonomous: true
requirements:
  - AUTHZ-03
  - AUTHZ-05
  - AUTHZ-06
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - CHAT-07
  - CHAT-08
  - CHAT-09
  - CHAT-10
  - CHAT-11
must_haves:
  truths:
    - "The assembled private-direct-chat flow covers D-01 through D-22 and all Phase 2 requirement IDs."
    - "A user can register, log in, start a private conversation, receive a non-streaming assistant response, reload history, delete with undo, and sign out."
    - "A second user cannot infer, read, append, retry, undo-delete, or delete the first user's conversation."
    - "Duplicate submit, stale principal state, provider failure, and unsafe Markdown all fail closed with no forbidden side effects."
  artifacts:
    - path: "backend/tests/smoke/test_private_direct_chat.py"
      provides: "Assembled Phase 2 smoke test through the local topology"
    - path: "backend/app/services/chat.py"
      provides: "Final send/retry/delete hardening if smoke exposes gaps"
    - path: "frontend/components/chat/ChatWorkspace.tsx"
      provides: "Final browser flow hardening if smoke exposes gaps"
  key_links:
    - from: "backend/tests/smoke/test_private_direct_chat.py"
      to: "frontend + Kong + FastAPI + PostgreSQL + fake provider"
      via: "docker compose up --build --wait smoke path"
      pattern: "private direct chat"
    - from: "frontend/lib/chat-api.ts"
      to: "backend/app/api/routes/chat.py"
      via: "same endpoint contract used by smoke"
      pattern: "/api/conversations"
---

<objective>
Create and run the assembled private-direct-chat smoke gate, then close any Phase 2 integration gaps found by the gate.

Purpose: Prove the separate backend, provider, UI, rendering, and authorization slices work together through the local Compose topology.
Output: Final smoke test, full validation run, and targeted fixes only where assembled evidence exposes a Phase 2 gap.
</objective>

## Phase Goal

**As a** signed-in user, **I want to** hold durable direct-LLM conversations, **so that** my chat history stays private to my own workspace.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-private-direct-chat/02-CONTEXT.md
@.planning/phases/02-private-direct-chat/02-RESEARCH.md
@.planning/phases/02-private-direct-chat/02-VALIDATION.md
@.planning/phases/02-private-direct-chat/02-AI-SPEC.md
@.planning/phases/02-private-direct-chat/02-UI-SPEC.md
@.planning/phases/02-private-direct-chat/02-05-SUMMARY.md
@.planning/phases/02-private-direct-chat/02-06-SUMMARY.md
@.planning/phases/01-secure-platform-and-account-access/01-VERIFICATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the assembled private-direct-chat smoke test</name>
  <files>backend/tests/smoke/test_private_direct_chat.py</files>
  <read_first>
    <file>.planning/phases/02-private-direct-chat/02-VALIDATION.md</file>
    <file>.planning/phases/02-private-direct-chat/02-AI-SPEC.md</file>
    <file>.planning/phases/02-private-direct-chat/02-UI-SPEC.md</file>
    <file>backend/tests/smoke/test_topology.py</file>
  </read_first>
  <action>
Create backend/tests/smoke/test_private_direct_chat.py as the assembled Phase 2 gate. It must start from the real local topology assumptions, authenticate a user, create a first conversation with initial_message and client_message_id, receive a completed assistant turn through a deterministic fake or test-configured provider boundary, reload ordered history, list newest-first conversations, soft-delete with hidden list state, exercise undo-delete, and verify sign-out/session cleanup remains compatible with the browser contract where the backend smoke can observe it. Include two-user BOLA checks, missing chat scope checks, duplicate client_message_id checks, provider failure with failed assistant state, and unsafe Markdown payload persistence. Do not require live external provider credentials for this deterministic smoke.
  </action>
  <acceptance_criteria>
    - "The smoke test names every Phase 2 critical path from 02-VALIDATION.md."
    - "The smoke test fails if provider failure creates completed assistant content."
    - "The smoke test fails if another user can observe or mutate private chat state."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/smoke/test_private_direct_chat.py; if ($LASTEXITCODE -eq 0) { throw "Expected assembled smoke to be RED until final integration gaps are closed" }</automated>
  </verify>
  <done>The assembled Phase 2 smoke contract exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run final validation and close assembled integration gaps</name>
  <files>backend/tests/smoke/test_private_direct_chat.py, backend/app/api/routes/chat.py, backend/app/services/chat.py, backend/app/db/repositories/conversations.py, backend/app/ai/chat_adapter.py, frontend/components/chat/ChatWorkspace.tsx, frontend/components/chat/ChatSidebar.tsx, frontend/components/chat/MessageMarkdown.tsx, frontend/lib/chat-api.ts</files>
  <read_first>
    <file>.planning/phases/02-private-direct-chat/02-VALIDATION.md</file>
    <file>.planning/phases/02-private-direct-chat/02-CONTEXT.md</file>
    <file>.planning/phases/02-private-direct-chat/02-AI-SPEC.md</file>
    <file>.planning/phases/02-private-direct-chat/02-UI-SPEC.md</file>
  </read_first>
  <action>
Run the complete Phase 2 validation contract and fix only defects that block the listed Phase 2 requirements or decisions. Confirm all locked decisions D-01 through D-22 are represented in behavior: chat-first workspace, desktop sidebar/mobile drawer, composer-first empty state, sidebar-bottom account menu, English copy, distinct SimpAgent branding, local title derivation, soft delete with undo, stable cursor pagination, Delete conversation as the only row management action, client_message_id idempotency, one in-flight assistant turn, retry reusing the original user message, inline failed assistant row with correlation ID, no fabricated assistant success, protected sequence ordering, GFM Markdown, raw HTML disabled, inert highlighted code blocks with copy, safe link schemes, external rel noopener noreferrer, and silent sanitizer blocking. If any final gate fails, make the smallest source fix in the files listed here and rerun that gate. Do not add new product controls or broaden Phase 2 boundaries.
  </action>
  <acceptance_criteria>
    - "All backend integration, security, unit, and smoke tests from 02-VALIDATION.md pass."
    - "Frontend chat workspace, Markdown tests, typecheck, and build pass."
    - "The assembled smoke proves no cross-user leakage, duplicate provider work, fabricated provider success, or unsafe Markdown execution."
    - "D-01 through D-22 and every Phase 2 requirement ID are traceable to passing tests or explicit UI behavior."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/integration/chat tests/security/test_chat_authorization.py tests/security/test_chat_idempotency.py tests/security/test_chat_provider_failure.py tests/security/test_chat_rendering_contract.py tests/unit/ai/test_chat_adapter.py -x && docker compose run --rm frontend npm run test -- tests/chat-workspace.test.ts tests/chat-markdown.test.ts tests/chat-session-routing.test.ts && docker compose run --rm frontend npm run typecheck && docker compose run --rm frontend npm run build && docker compose up --build --wait && docker compose run --rm backend pytest -q tests/smoke/test_private_direct_chat.py</automated>
  </verify>
  <done>Phase 2 is assembled, tested, and ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Full browser/API flow -> Compose topology | Authenticated chat traffic crosses frontend, Kong, FastAPI, PostgreSQL, and provider boundary |
| User A data -> User B probes | Private conversation IDs and message IDs may be guessed or reused across accounts |
| Stored content -> browser rendering | Persisted untrusted messages reload through the full application |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-22 | Information Disclosure | HIGH - blocking | assembled BOLA paths | mitigate | Smoke includes two-user list/retrieve/send/retry/delete/undo denial with no provider call |
| T-02-23 | Tampering | HIGH - blocking | duplicate submit/retry | mitigate | Smoke asserts one user row and one assistant success per client_message_id |
| T-02-24 | Information Disclosure | HIGH - blocking | provider/log/error data | mitigate | Validate safe correlation errors and no raw provider secret/error body exposure |
| T-02-25 | Elevation | HIGH - blocking | unsafe Markdown reload | mitigate | Stored adversarial content remains inert after reload |
| T-02-26 | Denial of Service | MEDIUM | provider/pending conflicts | mitigate | Pending-turn conflict and bounded provider timeout/retry validation |
| T-02-SC | Tampering | MEDIUM | package supply chain | mitigate | Confirm plan summaries for 02-02 and 02-06 recorded dependency legitimacy approvals before final signoff |
</threat_model>

## Artifacts this phase produces

- New `backend/tests/smoke/test_private_direct_chat.py`.
- Final verified endpoint set: `POST /api/conversations`, `GET /api/conversations`, `GET /api/conversations/{conversation_id}`, `DELETE /api/conversations/{conversation_id}`, `POST /api/conversations/{conversation_id}/undo-delete`, `POST /api/conversations/{conversation_id}/messages`, and `POST /api/conversations/{conversation_id}/messages/{client_message_id}/retry`.
- Final verified backend symbols: `OpenAIChatAdapter`, `ChatProviderError`, `ChatService`, `ConversationsRepository`, `ConversationSummary`, `ConversationDetail`, `ChatMessageResponse`.
- Final verified database fields and constraints: `messages.client_message_id`, `messages.status`, `ck_messages_message_status_known`, `ix_messages_conversation_client_message_id_unique`, `uq_messages_conversation_id_sequence_no`, and `conversations.deleted_at`.
- Final verified frontend symbols: `ChatWorkspace`, `ChatSidebar`, `ChatDrawer`, `ChatComposer`, `MessageMarkdown`, `CodeBlock`, `UndoToast`, and chat API helpers.

<verification>
Run the Task 2 automated command exactly. If any gate fails, fix the responsible Phase 2 source file and rerun the failed command plus the final assembled smoke.
</verification>

<success_criteria>
- Every Phase 2 requirement ID is covered by at least one passing test or final smoke assertion.
- D-01 through D-22 are implemented or explicitly enforced as absence of disallowed controls.
- The final user journey works through the local Docker Compose topology without ownership, idempotency, provider-failure, rendering, session, or secret-redaction regressions.
</success_criteria>

<output>
Create `.planning/phases/02-private-direct-chat/02-07-SUMMARY.md` when done.
</output>
