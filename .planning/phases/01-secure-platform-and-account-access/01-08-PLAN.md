---
phase: 01-secure-platform-and-account-access
plan: "08"
type: execute
wave: 6
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-06
  - 01-07
files_modified:
  - compose.yaml
  - backend/app/cli/bootstrap_admin.py
  - backend/app/cli/init_dev_secrets.py
  - backend/app/cli/seed_demo.py
  - backend/app/db/repositories/provisioning.py
  - backend/tests/integration/cli/test_provisioning.py
  - backend/tests/smoke/test_topology.py
  - frontend/app/page.tsx
  - frontend/components/account-access/AccountAccessShell.tsx
  - frontend/components/account-access/DemoAccountPanel.tsx
  - frontend/components/account-access/PlatformStatus.tsx
  - frontend/components/account-access/SecuritySummary.tsx
  - frontend/lib/demo-config.ts
  - frontend/lib/readiness.ts
  - frontend/tests/readiness.test.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-09
  - AUTH-10
  - AUTHZ-01
  - AUTHZ-08
must_haves:
  truths:
    - "This final plan runs only after every plan that can alter account UI, auth, session, readiness, schema, or topology behavior."
    - "D-04 provisions stable demo User/Admin accounts automatically only under explicit development mode and enabled seed; D-05 blocks mutation elsewhere."
    - "D-03 creates or promotes the first production Admin only through one explicit interactive one-time command."
    - "The UI shows exact ready/degraded/not-ready/disconnected and development-only demo states without exposing credentials in production artifacts."
    - "Fresh-volume Compose, full auth/session/security tests, topology smoke, migration checks, and secret scans pass as one assembled gate."
  artifacts:
    - "test_topology.py is the Wave 0 owner for PLAT-01/02 and final service/exposure evidence."
    - "Provisioning CLI/tests implement and prove D-03 through D-05."
    - "PlatformStatus, SecuritySummary, DemoAccountPanel, readiness, and demo-config complete the UI contract."
  key_links:
    - "Compose sequences postgres -> migrate -> development secret init/seed -> backend -> Kong/frontend while sandbox remains isolated."
    - "Next.js receives demo values only through a server-side dual gate and production output scans prove absence."
---

<objective>
Finish provisioning and the readiness/demo UI, then verify the complete Phase 1 topology and account/session lifecycle from a fresh volume.

Purpose: Produce final assembled evidence only after all relevant implementation plans are complete, preventing later UI/session edits from invalidating topology or demo results.
Output: Safe development provisioning, one-time production Admin bootstrap, complete readiness/demo presentation, and final Compose/security verification.
</objective>

## Phase Goal

**As a** developer or evaluator, **I want to** start, diagnose, and demonstrate the complete account platform safely, **so that** production credentials remain deliberate and every Phase 1 claim is reproducible.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-platform-and-account-access/01-CONTEXT.md
@.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md
@.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md
@.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md
@.planning/phases/01-secure-platform-and-account-access/01-UI-SPEC.md
@.planning/phases/01-secure-platform-and-account-access/01-SKELETON.md
@.planning/phases/01-secure-platform-and-account-access/01-06-SUMMARY.md
@.planning/phases/01-secure-platform-and-account-access/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED provisioning, readiness UI, and assembled topology contracts</name>
  <files>backend/tests/integration/cli/test_provisioning.py, backend/tests/smoke/test_topology.py, frontend/tests/readiness.test.ts</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-CONTEXT.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-UI-SPEC.md</file>
    <file>compose.yaml</file>
  </read_first>
  <action>
Write the final Wave 0 topology module plus provisioning and pure readiness mapping tests before implementation. Prove fresh-volume required services/jobs/dependencies/health/private ports, migration head, degraded startup without provider credentials, automatic seed idempotency, exact demo scope bundles, production seed refusal with zero mutation, interactive one-time Admin bootstrap, and safe output. Test every readiness aggregate/component mapping, visible-only polling inputs, malformed-state denial, demo dual gate, and production credential absence.
  </action>
  <acceptance_criteria>
    - "The topology test covers PLAT-01 and PLAT-02 and is RED before final Compose wiring."
    - "Provisioning tests assert database side effects and captured output, not only exit codes."
    - "Readiness tests encode every exact UI enable/disable mapping."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/integration/cli/test_provisioning.py tests/smoke/test_topology.py; $backendResult=$LASTEXITCODE; docker compose run --rm frontend npm run test -- frontend/tests/readiness.test.ts; if (($backendResult -eq 0) -and ($LASTEXITCODE -eq 0)) { throw "Expected final topology/provisioning/readiness tests to be RED" }</automated>
  </verify>
  <done>The final assembled and provisioning contract is executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce D-03 through D-05 and complete Compose startup jobs</name>
  <files>compose.yaml, backend/app/cli/bootstrap_admin.py, backend/app/cli/init_dev_secrets.py, backend/app/cli/seed_demo.py, backend/app/db/repositories/provisioning.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-CONTEXT.md</file>
    <file>backend/tests/integration/cli/test_provisioning.py</file>
    <file>backend/app/core/config.py</file>
    <file>backend/alembic/versions/0002_platform_foundations.py</file>
  </read_first>
  <action>
Implement a development-only initializer that generates local 3072-bit RSA and independent refresh/CSRF key files without overwriting explicit operator material. Implement D-04 demo User/Admin transactional upsert under an advisory lock, with exact User standard scopes and Admin standard plus `admin:read`/`admin:write`; reseeding is idempotent and revokes demo sessions only when credentials change. Enforce D-05 in mutation code: disabled seed is a safe no-op, enabled non-development seed fails with zero mutation. Implement D-03 as an explicit production operator command using interactive `getpass`, advisory lock, create-or-promote behavior, fail if an Admin already exists, safe output, and no startup invocation. Sequence the one-shot jobs after migration and before backend.
  </action>
  <acceptance_criteria>
    - "Development Compose produces exactly one stable demo User and Admin."
    - "Production seed attempts fail with zero mutation."
    - "Bootstrap succeeds once, never accepts password argv, and prints/logs no password."
    - "Backend startup never automatically creates a production Admin."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/integration/cli/test_provisioning.py tests/security/test_secret_leakage.py</automated>
  </verify>
  <done>D-03, D-04, and D-05 are enforced in code, database transactions, and Compose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete readiness/demo UI and run the final assembled gate</name>
  <files>frontend/app/page.tsx, frontend/components/account-access/AccountAccessShell.tsx, frontend/components/account-access/DemoAccountPanel.tsx, frontend/components/account-access/PlatformStatus.tsx, frontend/components/account-access/SecuritySummary.tsx, frontend/lib/demo-config.ts, frontend/lib/readiness.ts, frontend/tests/readiness.test.ts, backend/tests/smoke/test_topology.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-UI-SPEC.md</file>
    <file>backend/app/schemas/health.py</file>
    <file>frontend/components/account-access/AccountAccessShell.tsx</file>
    <file>backend/tests/smoke/test_topology.py</file>
  </read_first>
  <action>
Implement exact ready, degraded, not-ready, disconnected, component-label, retry, 60-second visible-only polling, responsive disclosure, verification rail, and security summary behavior. Ready/degraded enable account access; core-not-ready, network, malformed, and unknown aggregate states disable it. Render demo fill controls only when the Next.js server sees both development and enabled seed; fill without auto-submit and move focus/announce exactly. Production HTML, RSC, bundles, source maps, comments, and hidden content must contain no demo value. Then run a fresh-volume Compose build, full backend auth/session/security/schema suite, frontend type/tests, topology smoke, Alembic check, and canary scan.
  </action>
  <acceptance_criteria>
    - "All readiness/demo copy, mapping, polling, responsiveness, and accessibility match the approved UI contract."
    - "Provider-only degradation never blocks account access."
    - "Production artifacts contain no demo credential value."
    - "Fresh-volume assembled verification passes after every prior UI/auth/session plan."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend npm run typecheck &amp;&amp; docker compose run --rm frontend npm run test -- frontend/tests/auth-session.test.ts frontend/tests/readiness.test.ts &amp;&amp; docker compose run --rm frontend npm run build &amp;&amp; docker compose config -q &amp;&amp; docker compose down -v &amp;&amp; docker compose up --build --wait &amp;&amp; docker compose run --rm backend alembic check &amp;&amp; docker compose run --rm backend pytest -q</automated>
  </verify>
  <done>The final assembled Phase 1 platform is reproducible, diagnosable, and fully verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Compose startup -> privileged account mutation | Automatic jobs may create known or Admin credentials |
| Operator terminal -> bootstrap | Deliberate production action enters Admin authority |
| Next.js server -> client artifacts | Development demo values may leak publicly |
| Assembled edge -> private services | Final networks, ports, health, and ordering define exposure |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-30 | Elevation | HIGH - blocking | demo seed | mitigate | D-04/D-05 dual gate, advisory lock, exact scopes |
| T-01-31 | Elevation / Spoofing | HIGH - blocking | Admin bootstrap | mitigate | D-03 interactive one-time command, no startup/default credentials |
| T-01-32 | Information Disclosure | HIGH - blocking | frontend demo config | mitigate | Server dual gate and production artifact scans |
| T-01-33 | Denial of Service | HIGH - blocking | startup/readiness | mitigate | Health/completion dependencies and core/degraded distinction |
| T-01-34 | Tampering | HIGH - blocking | assembled topology | mitigate | Fresh-volume config/smoke/schema/full-suite gate |
</threat_model>

## Artifacts this phase produces

- Development secret initializer, demo seed, and production Admin bootstrap.
- Final Compose startup graph and topology smoke evidence.
- Platform status, security summary, demo panel, and readiness mapping/tests.
- Full assembled Phase 1 verification.

<verification>
Run Task 3, then separately run production-mode seed with `DEMO_SEED_ENABLED=true` and scan a production frontend build for demo canaries.
</verification>

<success_criteria>
- All 18 Phase 1 requirements and D-01 through D-05 have assembled evidence.
- Final verification occurs after every relevant implementation plan.
- Production has no automatic/default Admin or demo credential exposure.
- The approved UI and topology work from a fresh volume.
</success_criteria>

<output>
Create `.planning/phases/01-secure-platform-and-account-access/01-08-SUMMARY.md` when done.
</output>
