---
phase: 01-secure-platform-and-account-access
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/Dockerfile
  - compose.test.yaml
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/core/errors.py
  - backend/app/db/base.py
  - backend/app/db/session.py
  - backend/tests/conftest.py
  - backend/tests/fixtures/postgres.py
  - backend/tests/fixtures/auth.py
  - backend/tests/security/test_secret_leakage.py
  - backend/tests/smoke/test_account_access_skeleton.py
  - sandbox/Dockerfile
  - sandbox/healthcheck.py
autonomous: true
requirements:
  - PLAT-02
  - PLAT-04
must_haves:
  truths:
    - "Every Phase 1 integration and security test is forced onto migrated PostgreSQL 18 and cannot silently use SQLite."
    - "The FastAPI application, settings, error, and database boundaries are injectable and redact secret-bearing failures."
    - "The first full-stack contract is RED for missing product behavior, not for missing fixtures or skipped services."
    - "The sandbox foundation is health-only and has no execution, host, Docker, network, or secret authority."
  artifacts:
    - "compose.test.yaml provides the isolated PostgreSQL-backed test topology."
    - "backend/tests/conftest.py and fixtures provide keys, clock, app, client, auth, canary, and committed database state."
    - "backend/tests/smoke/test_account_access_skeleton.py specifies frontend -> Kong -> FastAPI -> PostgreSQL registration/login/me."
  key_links:
    - "The application factory consumes an injected immutable Settings object and injected database session factory."
    - "The PostgreSQL fixture applies Alembic head once available and rejects non-PostgreSQL URLs."
---

<objective>
Create the strictly required test and application foundation that all Phase 1 vertical slices execute against.

Purpose: Remove fixture, runtime, and isolation ambiguity before account behavior is implemented, while keeping the first real user path explicitly RED.
Output: Python/container test infrastructure, injectable FastAPI/database contracts, canary leakage checks, a failing full-stack skeleton test, and a health-only sandbox foundation.
</objective>

## Phase Goal

**As a** local user, **I want to** register, log in, and view my safe identity through the runnable platform, **so that** I can establish a protected account session without exposing long-lived credentials.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-secure-platform-and-account-access/01-CONTEXT.md
@.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md
@.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md
@.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md
@.planning/phases/01-secure-platform-and-account-access/01-SKELETON.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the PostgreSQL-only Wave 0 harness</name>
  <files>backend/pyproject.toml, backend/Dockerfile, compose.test.yaml, backend/tests/conftest.py, backend/tests/fixtures/postgres.py, backend/tests/fixtures/auth.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>
Create the audited Python 3.13 dependency/test configuration and backend test image. Configure pytest 9 and explicit pytest-asyncio behavior. Build fixtures for immutable settings, generated RSA keys, fixed time, canary secrets, application/client injection, PostgreSQL 18 migration state, deterministic committed cleanup, users, cookies, CSRF, and tokens. Reject SQLite and any transaction wrapper invisible to application-created sessions. This task owns the Wave 0 paths assigned to 01-01 in `01-VALIDATION.md`.
  </action>
  <acceptance_criteria>
    - "All fixture modules collect in the Python 3.13 test container."
    - "A non-PostgreSQL database URL fails immediately."
    - "Independent sessions can observe committed application state."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest --collect-only -q</automated>
  </verify>
  <done>The shared Wave 0 harness exists and enforces PostgreSQL-only evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Define injectable app, configuration, error, and database boundaries</name>
  <files>backend/app/main.py, backend/app/core/config.py, backend/app/core/errors.py, backend/app/db/base.py, backend/app/db/session.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md</file>
    <file>backend/tests/conftest.py</file>
  </read_first>
  <action>
Define `create_app`, immutable nested Pydantic settings, a bounded JSON error envelope, SQLAlchemy 2 typed declarative base, and async Psycopg session ownership. Settings must support `/run/secrets` and explicit `*_FILE` inputs as the canonical production mechanism, with direct secret values limited to local/test fallback per the resolved research decision. Redact secrets, DSNs, paths, and raw validation values. Do not add auth behavior, runtime `create_all()`, or provider generation.
  </action>
  <acceptance_criteria>
    - "Tests can inject settings, keys, clock, and database session factories."
    - "Settings repr and errors expose no canary secret or DSN."
    - "Application startup performs no schema creation and no external provider call."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test python -c "from app.main import create_app; from app.core.config import Settings; assert create_app"</automated>
  </verify>
  <done>Later plans have stable app, configuration, error, and transaction entry points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record secret and full-stack RED contracts and isolate the sandbox foundation</name>
  <files>backend/tests/security/test_secret_leakage.py, backend/tests/smoke/test_account_access_skeleton.py, sandbox/Dockerfile, sandbox/healthcheck.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-SKELETON.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-UI-SPEC.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md</file>
  </read_first>
  <action>
Create canary assertions reusable across responses, logs, events, CLI output, and generated frontend artifacts. Write the failing walking-skeleton test for a browser-visible page plus registration, login, and `/me` through the public Kong route with direct PostgreSQL side-effect checks. Create a non-root sandbox image exposing only a bounded health check; include no execution route, interpreter control API, application secret, app network, host mount, Docker socket, device, or privilege. The skeleton test must fail because Plans 01-02 and 01-03 are absent.
  </action>
  <acceptance_criteria>
    - "Secret leakage tests collect and provide canary helpers to later plans."
    - "The skeleton test names the complete frontend-to-database journey and is RED."
    - "The sandbox image contains health behavior only."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/security/test_secret_leakage.py tests/smoke/test_account_access_skeleton.py; if ($LASTEXITCODE -eq 0) { throw "Expected the unimplemented walking skeleton to remain RED" }</automated>
  </verify>
  <done>Wave 0 records the first vertical outcome and the sandbox boundary without implementing product behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test runner -> PostgreSQL | Tests must observe production transaction and lock semantics |
| Environment/secret files -> Settings | Operator values enter security-critical policy |
| Host -> sandbox foundation | A health service must not gain code-execution authority |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | HIGH - blocking | test database | mitigate | PostgreSQL-only URL guard, Alembic head, committed cleanup |
| T-01-02 | Information Disclosure | HIGH - blocking | settings/errors/tests | mitigate | Secret types, bounded envelopes, canary assertions |
| T-01-03 | Elevation | HIGH - blocking | sandbox foundation | mitigate | Non-root health-only image, no execution/API/socket/mount/secret |
| T-01-SC | Tampering | HIGH - blocking | dependencies/images | mitigate | Only audited versions and official images from research |
</threat_model>

## Artifacts this phase produces

- Wave 0 runtime and PostgreSQL fixtures.
- Injectable application/configuration/database contracts.
- Secret-canary and full-stack RED tests.
- Health-only sandbox foundation.

<verification>
Run all three task commands and confirm no application account route exists yet.
</verification>

<success_criteria>
- Every later test plan can run against PostgreSQL 18.
- The walking skeleton is deterministically RED for missing behavior.
- Secret and sandbox boundaries exist before account implementation.
</success_criteria>

<output>
Create `.planning/phases/01-secure-platform-and-account-access/01-01-SUMMARY.md` when done.
</output>
