---
phase: 01-secure-platform-and-account-access
plan: "07"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .env.example
  - backend/alembic/versions/0002_platform_foundations.py
  - backend/app/api/routes/health.py
  - backend/app/core/config.py
  - backend/app/core/provider_status.py
  - backend/app/models/domain.py
  - backend/app/models/evidence.py
  - backend/app/schemas/health.py
  - backend/tests/unit/test_config.py
  - backend/tests/integration/db/test_migrations.py
  - backend/tests/integration/test_health.py
  - backend/tests/integration/test_provider_status.py
autonomous: true
requirements:
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06
must_haves:
  truths:
    - "A fresh PostgreSQL database reaches a reviewed Alembic head containing every PLAT-03 table, constraint, index, and foreign-key action."
    - "Production settings reject unsafe origins, cookies, demo mode, placeholders, loopback providers, missing keys, and secret-bearing validation output."
    - "`/health` is dependency-free; `/ready` returns 503 for database/migration failure and sanitized 200 degraded for provider-only failure."
    - "No Gemini model is hardcoded; configured model status is metadata-only and Phase 3 owns the live Search grounding capability check."
  artifacts:
    - "0002_platform_foundations.py completes conversation, message, tool, audit, and evidence schema foundations without routes."
    - "test_config.py, test_migrations.py, test_health.py, and test_provider_status.py are RED before implementation."
    - ".env.example documents every environment and secret-file setting without real secrets."
  key_links:
    - "Readiness combines database/Alembic core state with closed provider component states."
    - "Production consumes `/run/secrets`/`*_FILE`; development generation is implemented in 01-08."
---

<objective>
Complete the durable schema, environment invariants, liveness/readiness contract, and sanitized provider degradation behavior.

Purpose: Make the account platform reproducible and diagnosable without coupling authentication availability to external AI providers or exposing configuration details.
Output: Complete Phase 1 schema migration, hardened settings/template, provider status registry, readiness schemas/routes, and PostgreSQL/configuration tests.
</objective>

## Phase Goal

**As a** developer, **I want to** distinguish healthy, degraded, and blocked platform states, **so that** account access remains diagnosable without leaking secrets.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md
@.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md
@.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md
@.planning/phases/01-secure-platform-and-account-access/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED configuration, migration, health, and provider-status evidence</name>
  <files>backend/tests/unit/test_config.py, backend/tests/integration/db/test_migrations.py, backend/tests/integration/test_health.py, backend/tests/integration/test_provider_status.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md</file>
    <file>backend/alembic/versions/0001_account_access.py</file>
    <file>backend/app/core/config.py</file>
  </read_first>
  <action>
Create all four assigned Wave 0 modules before implementation. Inspect the real PostgreSQL schema, named constraints, index predicates, FK actions, disposable downgrade/re-upgrade, and `alembic check`. Exercise production settings invariants and secret redaction. Verify dependency-free liveness, database/migration not-ready responses, provider-only degraded responses, unknown-state fail-closed mapping, and fake provider exceptions containing API keys, DSNs, endpoints, model IDs, filenames, and canaries.
  </action>
  <acceptance_criteria>
    - "Tests use PostgreSQL 18 and real migration introspection."
    - "Provider/config canaries are absent from responses and logs."
    - "All four modules are RED before Tasks 2-3."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/unit/test_config.py tests/integration/db/test_migrations.py tests/integration/test_health.py tests/integration/test_provider_status.py; if ($LASTEXITCODE -eq 0) { throw "Expected platform tests to be RED" }</automated>
  </verify>
  <done>The complete platform configuration/schema/readiness contract is executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the reviewed schema and production configuration invariants</name>
  <files>.env.example, backend/alembic/versions/0002_platform_foundations.py, backend/app/core/config.py, backend/app/models/domain.py, backend/app/models/evidence.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md</file>
    <file>backend/tests/unit/test_config.py</file>
    <file>backend/tests/integration/db/test_migrations.py</file>
    <file>backend/alembic/versions/0001_account_access.py</file>
  </read_first>
  <action>
Add typed conversation, message, tool execution, and audit foundations plus remaining security-event fields, then create a manually reviewed second revision with all named checks, indexes, JSONB defaults, stable ordering keys, correlation indexes, and explicit delete actions required by PLAT-03. Add no chat/tool/admin routes. Complete immutable settings and `.env.example` for environment, demo gates, database secret file, JWT profile/key files, refresh/CSRF files, origins, cookie/session lifetimes, provider endpoints/keys/models/timeouts, and production invariants. Canonical production secrets are `/run/secrets`/`*_FILE`; direct values are local/test fallback only.
  </action>
  <acceptance_criteria>
    - "Fresh upgrade, introspection, downgrade/re-upgrade, and `alembic check` pass."
    - "All PLAT-03 tables exist without later-phase behavior."
    - "Production rejects every unsafe configuration case with redacted errors."
    - "The template contains placeholders/comments but no real secret."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/unit/test_config.py tests/integration/db/test_migrations.py &amp;&amp; docker compose -f compose.test.yaml run --rm backend-test alembic check</automated>
  </verify>
  <done>The database and configuration contracts are complete and reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement sanitized readiness and configured provider degradation</name>
  <files>backend/app/api/routes/health.py, backend/app/core/provider_status.py, backend/app/schemas/health.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md</file>
    <file>backend/tests/integration/test_health.py</file>
    <file>backend/tests/integration/test_provider_status.py</file>
    <file>backend/app/core/config.py</file>
  </read_first>
  <action>
Keep `/health` at `200 {"status":"alive"}` with no dependency calls. Implement closed readiness/component enums, database connectivity and Alembic-head checks, and bounded injected provider checkers. Return `503 not_ready` for database/migration failure and `200 degraded` for unconfigured/unavailable LLM or Search. Read `SEARCH_MODEL` only from settings; optionally use bounded `google-genai` model metadata retrieval, never generation or Search, and never hardcode/fallback a model. Unknown states become unavailable plus redacted evidence.
  </action>
  <acceptance_criteria>
    - "Core failures and provider degradation produce the required distinct status codes."
    - "Readiness contains only bounded states/reasons and correlation data."
    - "No endpoint, model ID, DSN, filename, key, exception, or canary is exposed."
    - "Account APIs remain usable during provider-only degradation."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/integration/test_health.py tests/integration/test_provider_status.py tests/security/test_secret_leakage.py</automated>
  </verify>
  <done>The platform reports safe, actionable core and provider state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Environment/secrets -> Settings | Operator configuration becomes runtime policy |
| Dependencies/providers -> `/ready` | Internal failures may become public status |
| Models -> Alembic -> PostgreSQL | Schema intent becomes durable constraints |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-26 | Information Disclosure | HIGH - blocking | settings/readiness | mitigate | Secret files/types and bounded reason enums |
| T-01-27 | Denial of Service | HIGH - blocking | readiness | mitigate | Core 503 vs provider degraded 200, bounded checks |
| T-01-28 | Tampering | HIGH - blocking | migration | mitigate | Reviewed revisions, introspection, alembic check |
| T-01-29 | Elevation | HIGH - blocking | unknown state | mitigate | Closed enums and fail-closed evidence |
| T-01-SC | Tampering | HIGH - blocking | provider dependency | mitigate | Audited metadata client only; no generation/search |
</threat_model>

## Artifacts this phase produces

- Complete Phase 1 schema and environment template.
- Production configuration invariants.
- Liveness/readiness/provider status contracts and tests.

<verification>
Run all task commands and inspect readiness with database down, migration pending, and fake provider failures.
</verification>

<success_criteria>
- PLAT-03 through PLAT-06 have PostgreSQL-backed evidence.
- Provider absence degrades only provider features.
- Configuration and readiness disclose no secrets or raw diagnostics.
</success_criteria>

<output>
Create `.planning/phases/01-secure-platform-and-account-access/01-07-SUMMARY.md` when done.
</output>
