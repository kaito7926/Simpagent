---
phase: 01-secure-platform-and-account-access
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/0001_account_access.py
  - backend/app/models/account.py
  - backend/app/models/session.py
  - backend/app/models/evidence.py
  - backend/app/schemas/auth.py
  - backend/app/security/passwords.py
  - backend/app/security/access_tokens.py
  - backend/app/services/account_access.py
  - backend/app/api/routes/auth.py
  - backend/app/api/routes/health.py
  - backend/tests/integration/auth/test_registration.py
  - backend/tests/integration/auth/test_login.py
  - backend/tests/integration/auth/test_me.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-08
must_haves:
  truths:
    - "A PostgreSQL-backed API user can register, log in, and retrieve safe current identity."
    - "D-01 and D-02 are enforced from the first write: active User, fixed role, exact standard scopes, and no client authority fields."
    - "Registration is normalized and non-enumerating, passwords are Argon2id hashes, and login creates protected refresh/CSRF cookies plus a short RS256 access token."
    - "`/me` returns only safe identity attributes and never credential/session material."
  artifacts:
    - "0001_account_access.py creates account, identity, credential, scope, refresh, and security-event foundations."
    - "test_registration.py, test_login.py, and test_me.py are written RED before implementation."
    - "auth.py exposes register, login, and me through typed schemas and services."
  key_links:
    - "Account operations use application-created SQLAlchemy sessions against migrated PostgreSQL."
    - "Registration request schemas contain only email and password; role/scopes/active/demo are server-owned."
---

<objective>
Implement the first real PostgreSQL account interaction: registration, login, and safe current identity through FastAPI.

Purpose: Establish the backend half of the walking skeleton with D-01/D-02 invariants and real durable state before the browser/topology plan connects it.
Output: Reviewed account/session migration, account models, password/token primitives, auth routes, liveness baseline, and green PostgreSQL lifecycle tests.
</objective>

## Phase Goal

**As a** local user, **I want to** create and authenticate an account backed by PostgreSQL, **so that** the browser can later prove a real protected end-to-end session.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-platform-and-account-access/01-CONTEXT.md
@.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md
@.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md
@.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md
@.planning/phases/01-secure-platform-and-account-access/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED registration, login, and current-user contracts</name>
  <files>backend/tests/integration/auth/test_registration.py, backend/tests/integration/auth/test_login.py, backend/tests/integration/auth/test_me.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-CONTEXT.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-VALIDATION.md</file>
    <file>backend/tests/fixtures/postgres.py</file>
    <file>backend/tests/fixtures/auth.py</file>
  </read_first>
  <action>
Write PostgreSQL integration tests before production code. Cover normalized and normalized-equivalent duplicate registration, forbidden authority fields, exact D-01/D-02 defaults, Argon2id storage, generic registration acceptance, valid/generic-invalid login, strict access-token shape, protected refresh/CSRF cookie attributes, and safe `/me` fields. Use independent committed sessions and canary values. These files are the explicit Wave 0 owners for AUTH-01 through AUTH-04 and AUTH-08.
  </action>
  <acceptance_criteria>
    - "Tests collect against PostgreSQL 18 and fail because account behavior is absent."
    - "Duplicate registration has identical status/body and one persisted account."
    - "No assertion depends on SQLite, mocked persistence, or decoded browser JWT data."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/integration/auth/test_registration.py tests/integration/auth/test_login.py tests/integration/auth/test_me.py; if ($LASTEXITCODE -eq 0) { throw "Expected account lifecycle tests to be RED" }</automated>
  </verify>
  <done>The real account lifecycle is specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the reviewed account and session persistence foundation</name>
  <files>backend/alembic.ini, backend/alembic/env.py, backend/alembic/versions/0001_account_access.py, backend/app/models/account.py, backend/app/models/session.py, backend/app/models/evidence.py, backend/app/security/passwords.py, backend/app/security/access_tokens.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-RESEARCH.md</file>
    <file>.planning/phases/01-secure-platform-and-account-access/01-PATTERNS.md</file>
    <file>backend/tests/integration/auth/test_registration.py</file>
    <file>backend/app/db/base.py</file>
  </read_first>
  <action>
Create typed SQLAlchemy models and a manually reviewed Alembic revision for users, scopes, identities, local credentials, refresh families/tokens, and security events. Use UUIDs, timestamptz, named checks/uniques/FKs/indexes, unique `(issuer, subject)`, unique normalized email key, exact known role/scope checks, refresh lineage, and HMAC digest storage columns. Implement NFC 15-128 code-point password policy with bounded bytes, local blocklist, `pwdlib[argon2]`, real-or-dummy verification support, and strict ten-minute RS256 `at+jwt` issuance primitives. Do not use `create_all()`, plaintext credentials, or JWT refresh tokens.
  </action>
  <acceptance_criteria>
    - "A fresh PostgreSQL database upgrades to the account-access revision."
    - "Generated password hashes start with `$argon2id$`."
    - "Database constraints prevent unknown roles/scopes and duplicate identity/refresh lineage."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test alembic upgrade head</automated>
  </verify>
  <done>The durable account/session foundation exists with reviewed security constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make registration, login, and me pass with fixed account authority</name>
  <files>backend/app/schemas/auth.py, backend/app/services/account_access.py, backend/app/api/routes/auth.py, backend/app/api/routes/health.py</files>
  <read_first>
    <file>.planning/phases/01-secure-platform-and-account-access/01-UI-SPEC.md</file>
    <file>backend/tests/integration/auth/test_registration.py</file>
    <file>backend/tests/integration/auth/test_login.py</file>
    <file>backend/tests/integration/auth/test_me.py</file>
  </read_first>
  <action>
Implement typed register/login/me transport and one service-owned transaction per use case. Registration accepts only email/password, normalizes consistently, performs Argon2id work before duplicate resolution, catches the unique race, and always applies D-01/D-02. Login uses real-or-dummy verification, creates one family/token with only an HMAC digest stored, issues the strict short access token, and sets `__Host-simpagent_refresh` HttpOnly plus readable family-bound CSRF cookie. `/me` validates the initial token profile and reloads the active user. Return stable bounded envelopes only. Add dependency-free `/health`; complete readiness belongs to 01-07.
  </action>
  <acceptance_criteria>
    - "The three lifecycle modules pass against migrated PostgreSQL."
    - "New and duplicate registration are indistinguishable and preserve exact D-01/D-02 state."
    - "Plaintext passwords, raw refresh tokens, cookies, and keys are absent from responses, logs, and storage."
    - "`/me` returns only id, normalized email, role, sorted scopes, and active status."
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/integration/auth/test_registration.py tests/integration/auth/test_login.py tests/integration/auth/test_me.py tests/security/test_secret_leakage.py</automated>
  </verify>
  <done>The backend account-access slice is real, durable, and ready for browser/topology wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP input -> account service | Untrusted email/password and extra fields enter account creation/authentication |
| Service -> PostgreSQL | Credentials, authority, and refresh state become durable |
| Claims -> `/me` | Signed token data is compared with current account state |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Elevation | HIGH - blocking | registration | mitigate | D-01/D-02 server defaults, extra-forbid schemas, DB checks |
| T-01-05 | Information Disclosure | HIGH - blocking | registration/login | mitigate | Generic outcomes, dummy hash, canary scans |
| T-01-06 | Spoofing | HIGH - blocking | access token | mitigate | RS256 typed token with required claims; complete corpus in 01-04 |
| T-01-07 | Tampering | HIGH - blocking | migration | mitigate | Named constraints, reviewed SQL, PostgreSQL tests |
| T-01-SC | Tampering | HIGH - blocking | auth dependencies | mitigate | Only audited PyJWT, pwdlib, email-validator, SQLAlchemy, Alembic |
</threat_model>

## Artifacts this phase produces

- Account/session/evidence models and initial migration.
- Registration, login, `/me`, and `/health` routes.
- Password/access-token primitives.
- PostgreSQL lifecycle tests.

<verification>
Run the Task 3 command and inspect the database for one fixed-default account after duplicate registration.
</verification>

<success_criteria>
- A real PostgreSQL account can register, authenticate, and retrieve safe identity.
- D-01 and D-02 cannot be overridden.
- Credential material is not returned, logged, or stored in plaintext.
</success_criteria>

<output>
Create `.planning/phases/01-secure-platform-and-account-access/01-02-SUMMARY.md` when done.
</output>
