---
phase: 04-isolated-python-execution
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - backend/app/api/routes/chat.py
  - backend/app/api/routes/python.py
  - backend/app/agent/coordinator.py
  - backend/app/agent/decisions.py
  - backend/app/agent/policy.py
  - backend/app/tools/python_client.py
  - backend/app/services/python_sessions.py
  - backend/app/security/tool_capabilities.py
  - backend/app/core/config.py
  - backend/app/db/repositories/python_state.py
  - backend/app/main.py
  - backend/tests/integration/python/test_python_authorization.py
  - backend/tests/integration/python/test_python_limits.py
  - backend/tests/security/test_python_request_hardening.py
autonomous: true
requirements:
  - CHAT-12
  - SBOX-02
  - SBOX-05
  - SBOX-06
  - SBOX-07
decisions_covered:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-15
  - D-16
  - D-17
  - D-18
must_haves:
  truths:
    - "Tool authorization is rechecked immediately before Python execution, and model output cannot authorize or mutate runtime policy."
    - "The backend signs one execution capability per run, defaults to `python-basic-v1`, and elevates to `python-data-v1` only through backend-owned narrow rules."
    - "Conversation-scoped Python state and reviewed artifacts share a 15-minute sliding inactivity window and never become global or indefinite worker state."
    - "Completed user-code exceptions remain execution results and must not be remapped to `infra_failure`."
  artifacts:
    - "Capability-signing helper, internal Python client, and approved artifact download route."
    - "Coordinator/policy integration for one-tool-per-turn and exact Python status mapping."
    - "Integration and security tests for authorization, limit reporting, and request hardening."
  key_links:
    - "The backend must consume the supervisor and runtime shape implemented in 04-02 without widening the trust boundary."
    - "All frontend Python rendering in 04-04 depends on the exact result and status envelope finalized here."
---

<objective>
Integrate Python execution into backend policy, coordinator, and chat-tool orchestration.

Purpose: connect the trusted sandbox boundary to real authorization, state, and execution-evidence flow without letting model or caller input widen runtime authority.
Output: capability-signing helper, Python client, coordinator/policy integration, expiring conversation-scoped state, and green authorization/limit/hardening tests.
</objective>

## Phase Goal

**As an** authorized user, **I want to** run bounded Python through the chat workflow, **so that** I can calculate or transform data without gaining access to the host, application network, or secrets.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-isolated-python-execution/04-CONTEXT.md
@.planning/phases/04-isolated-python-execution/04-RESEARCH.md
@.planning/phases/04-isolated-python-execution/04-PATTERNS.md
@.planning/phases/04-isolated-python-execution/04-VALIDATION.md
@.planning/phases/04-isolated-python-execution/04-01-SUMMARY.md
@.planning/phases/04-isolated-python-execution/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED integration and hardening tests for backend Python orchestration</name>
  <files>backend/tests/integration/python/test_python_authorization.py, backend/tests/integration/python/test_python_limits.py, backend/tests/security/test_python_request_hardening.py</files>
  <read_first>
    <file>.planning/phases/04-isolated-python-execution/04-VALIDATION.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-02-SUMMARY.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-CONTEXT.md</file>
  </read_first>
  <action>
Add RED tests for permission checks, one-tool-per-turn enforcement, reviewed-profile selection, exact terminating-limit mapping, and request hardening against caller-controlled runtime mutation. These tests must prove that denied or malformed Python requests never reach the supervisor, that default profile selection stays on `python-basic-v1` unless a narrow backend rule allows `python-data-v1`, and that execution evidence remains correct.
  </action>
  <acceptance_criteria>
    - "Authorization tests cover missing scope, one-tool-per-turn violation, deny-and-explain behavior for Search-plus-Python prompts, and backend-owned profile selection."
    - "Limit tests require the exact terminating limit to be returned and persisted."
    - "Hardening tests reject any request shape that attempts to control image, network, mounts, commands, or package policy."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/integration/python/test_python_authorization.py tests/integration/python/test_python_limits.py tests/security/test_python_request_hardening.py</automated>
  </verify>
  <done>The backend integration contract is RED and security-specific before orchestration code lands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement capability signing, Python client, and expiring conversation state</name>
  <files>backend/app/tools/python_client.py, backend/app/services/python_sessions.py, backend/app/security/tool_capabilities.py, backend/app/core/config.py, backend/app/db/repositories/python_state.py</files>
  <read_first>
    <file>backend/app/security/access_tokens.py</file>
    <file>backend/app/services/sessions.py</file>
    <file>.planning/phases/04-isolated-python-execution/04-01-SUMMARY.md</file>
  </read_first>
  <action>
Implement the backend helper layers that sign a one-shot Python execution capability, call the supervisor through a typed internal client, persist `ToolExecution` state transitions, and restore/expire bounded conversation-scoped Python state. The retention window must enforce the locked 15-minute sliding conversation window, reviewed artifacts must expire with the same session semantics, and profile selection must remain backend-owned.
  </action>
  <acceptance_criteria>
    - "Capability tokens are short-lived, one-execution, and tied to the bounded input/profile contract."
    - "Python state and reviewed artifact retention are conversation-scoped and use a 15-minute sliding inactivity window."
    - "Expired state restores from a clean environment, and expired artifact downloads are rejected through the approved API path."
    - "No service method allows the model or caller to override reviewed profile names or runtime policy."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend python -c "from app.security.tool_capabilities import issue_python_capability; print(callable(issue_python_capability))"</automated>
  </verify>
  <done>The backend can authorize, sign, call, and track Python execution without widening policy authority.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Python into the coordinator, chat route, and artifact download path</name>
  <files>backend/app/api/routes/chat.py, backend/app/api/routes/python.py, backend/app/main.py, backend/app/agent/coordinator.py, backend/app/agent/decisions.py, backend/app/agent/policy.py</files>
  <read_first>
    <file>.planning/phases/04-isolated-python-execution/04-RESEARCH.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-UI-SPEC.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-02-SUMMARY.md</file>
  </read_first>
  <action>
Extend the chat/coordinator flow so natural-language prompts may propose Python, but backend policy decides whether Python runs. Add the owner-scoped approved artifact download path, including expiry handling for stale downloads. Map success, deny, policy error, limit, and infrastructure-failure outcomes into the exact envelope agreed in 04-01, while keeping completed user-code exceptions out of the infrastructure-failure path. Preserve one-tool-per-turn behavior and the deny-and-explain fallback when a prompt would require Search plus Python without enough local data.
  </action>
  <acceptance_criteria>
    - "Coordinator policy rechecks `tool:python` immediately before execution."
    - "Default profile selection uses `python-basic-v1`, and elevation to `python-data-v1` happens only for clearly data-oriented or approved artifact-producing runs."
    - "Search-plus-Python prompts follow the locked v1 deny/explain behavior when they need external data."
    - "Expired artifact download requests return `410 Gone` without leaking file payloads across owners."
    - "Completed user-code exceptions remain distinct from `infra_failure`, and every requested, denied, running, succeeded, limit-reached, policy-error, or infrastructure-failure action persists through `ToolExecution`."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/integration/python/test_python_authorization.py tests/integration/python/test_python_limits.py</automated>
  </verify>
  <done>The backend chat path can safely authorize, execute, and record Python work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Model proposal -> backend policy | Untrusted tool proposal must not become authorization |
| Backend -> supervisor request | Capability-bound internal execution call |
| Conversation state -> subsequent execution | Retained state must not leak across owners or persist indefinitely |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-08 | Elevation | HIGH - blocking | coordinator/policy | mitigate | immediate scope recheck, one-tool-per-turn enforcement, no model authorization |
| T-04-09 | Tampering | HIGH - blocking | internal execution request | mitigate | signed one-shot capability and request hardening tests |
| T-04-10 | Information Disclosure | HIGH - blocking | conversation-scoped state | mitigate | explicit ownership, expiry, and bounded restored context |
| T-04-11 | Repudiation | HIGH - blocking | execution evidence | mitigate | persistent `ToolExecution` state transitions for every outcome |
</threat_model>

## Artifacts this phase produces

- Python capability-signing helper and internal supervisor client.
- Conversation-scoped Python state service with explicit sliding expiry.
- Coordinator, artifact-route, and chat integration for Python execution and deny/limit mapping.

<verification>
Run authorization, limit, and request-hardening tests and confirm denied requests never produce supervisor side effects.
</verification>

<success_criteria>
- Backend policy remains the only authority for whether Python may run.
- Python execution state and reviewed artifacts are explicit, persisted, and expiring on the locked session window.
- The exact status envelope is ready for frontend rendering.
</success_criteria>

<output>
Create `.planning/phases/04-isolated-python-execution/04-03-SUMMARY.md` when done.
</output>
