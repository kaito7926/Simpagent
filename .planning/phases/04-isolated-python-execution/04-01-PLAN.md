---
phase: 04-isolated-python-execution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/domain.py
  - backend/app/models/python_state.py
  - backend/app/schemas/python.py
  - backend/app/db/repositories/python_state.py
  - backend/alembic/versions/0003_python_execution_contracts.py
  - backend/tests/unit/python/test_result_envelope.py
  - backend/tests/integration/python/test_execution_contracts.py
  - backend/tests/security/test_python_backend_boundary.py
autonomous: true
requirements:
  - CHAT-12
  - SBOX-01
  - SBOX-07
decisions_covered:
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - D-17
  - D-18
must_haves:
  truths:
    - "FastAPI never evaluates user Python directly; it only records intent, policy, and typed execution state."
    - "Python result, artifact, and limit metadata use one backend-owned contract that later backend and frontend work can share without reinterpretation."
    - "Conversation-scoped Python state and reviewed artifact persistence are bounded from the beginning with one shared expiry model rather than retrofitted after runtime work exists."
  artifacts:
    - "Typed backend Python result envelope and approved artifact metadata schema."
    - "Migration and repository contract for conversation-scoped Python state and artifact retention."
    - "RED tests that prove the backend boundary and typed execution contract before the sandbox exists."
  key_links:
    - "Later plans must consume the same result envelope emitted here."
    - "Every later Python path writes through `ToolExecution` and any new bounded Python-state persistence created here."
---

<objective>
Define the bounded Python execution contract before any runtime or UI work starts.

Purpose: lock the state, artifact, and result semantics so later sandbox, backend, and frontend plans implement against one stable shape.
Output: typed Python execution DTOs, persistence/migration seams for short-lived state and approved artifacts, and RED tests proving the backend never executes user code directly.
</objective>

## Phase Goal

**As an** authorized user, **I want to** run bounded Python through the chat workflow, **so that** I can calculate or transform data without gaining access to the host, application network, or secrets.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-isolated-python-execution/04-CONTEXT.md
@.planning/phases/04-isolated-python-execution/04-UI-SPEC.md
@.planning/phases/04-isolated-python-execution/04-RESEARCH.md
@.planning/phases/04-isolated-python-execution/04-PATTERNS.md
@.planning/phases/04-isolated-python-execution/04-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED contract and boundary tests for Python execution</name>
  <files>backend/tests/unit/python/test_result_envelope.py, backend/tests/integration/python/test_execution_contracts.py, backend/tests/security/test_python_backend_boundary.py</files>
  <read_first>
    <file>.planning/phases/04-isolated-python-execution/04-CONTEXT.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-RESEARCH.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-VALIDATION.md</file>
  </read_first>
  <action>
Create RED tests for the typed Python result envelope, approved artifact metadata, and exact status taxonomy required by `CHAT-12` and `SBOX-07`. Add a backend-boundary test that fails if any Phase 4 implementation path evaluates user code in-process, shells out from FastAPI, or bypasses the future supervisor boundary. These tests should define the contract, not guess at a runtime implementation.
  </action>
  <acceptance_criteria>
    - "Result-envelope tests fail only because Python execution DTOs and bounded metadata are not implemented yet."
    - "The contract locks completed user-code exceptions as execution results that stay distinct from `infra_failure`."
    - "The backend-boundary test names forbidden behaviors such as `exec`, `eval`, direct subprocess Python execution, or shell-mediated code execution."
    - "The RED suite creates no dependency on the yet-to-exist sandbox runtime."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/unit/python/test_result_envelope.py tests/integration/python/test_execution_contracts.py tests/security/test_python_backend_boundary.py</automated>
  </verify>
  <done>Phase 4 has explicit RED coverage for the execution contract and the backend trust boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded persistence contracts for Python state and artifacts</name>
  <files>backend/app/models/domain.py, backend/app/models/python_state.py, backend/app/db/repositories/python_state.py, backend/alembic/versions/0003_python_execution_contracts.py</files>
  <read_first>
    <file>backend/app/models/domain.py</file>
    <file>backend/app/models/evidence.py</file>
    <file>.planning/phases/04-isolated-python-execution/04-PATTERNS.md</file>
  </read_first>
  <action>
Add the persistence layer for conversation-scoped Python state and approved artifact metadata. Keep `ToolExecution` as the canonical audit row and add new tables only for bounded state snapshots and artifact records that do not fit cleanly into existing models. The migration must enforce explicit size/status/ownership constraints, shared state/artifact expiry semantics, and must not introduce user-controlled policy fields such as arbitrary image, command, mount, network, or package settings.
  </action>
  <acceptance_criteria>
    - "Conversation ownership and the shared 15-minute sliding expiry window are explicit in the persistence contract."
    - "Artifact persistence supports bounded expiry and later `410 Gone` handling without losing audit ownership."
    - "Artifact persistence cannot represent unapproved arbitrary file system surfaces."
    - "No schema field allows caller-controlled runtime policy mutation."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend pytest -q tests/integration/db/test_migrations.py -k python</automated>
  </verify>
  <done>Later plans have stable storage seams for Python state, artifacts, and execution evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Define the typed Python execution DTOs and repository seams</name>
  <files>backend/app/schemas/python.py, backend/app/db/repositories/python_state.py</files>
  <read_first>
    <file>backend/app/schemas/auth.py</file>
    <file>backend/app/core/errors.py</file>
    <file>.planning/phases/04-isolated-python-execution/04-UI-SPEC.md</file>
  </read_first>
  <action>
Create the DTOs and repository interfaces that represent Python acceptance, running, success, deny, limit, policy-error, and infrastructure-failure states. The envelope must expose only bounded stdout/stderr excerpts, approved artifact metadata, duration, profile name, and exact terminating limit when applicable. It must also preserve completed user-code exception semantics without collapsing them into infrastructure failure. It must not expose host paths, container IDs, raw stack traces, or implementation-only runtime fields.
  </action>
  <acceptance_criteria>
    - "DTOs map directly to the UI states approved in `04-UI-SPEC.md`."
    - "The contract distinguishes deny, policy error, limit reached, and infrastructure failure."
    - "Approved artifact metadata stays API-relative and compatible with bounded expiry handling."
    - "Repository interfaces are narrow enough that later plans can implement them without codebase re-discovery."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend python -c "from app.schemas.python import PythonExecutionResult; print(PythonExecutionResult.model_json_schema()['title'])"</automated>
  </verify>
  <done>The backend contract for all later Python work is fixed and importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| FastAPI -> Python contract layer | The application decides whether Python may run and what bounded metadata is legal |
| Database -> bounded execution state | Persisted Python state and artifact metadata must not become an unsafe capability surface |
| Backend code -> future supervisor | Direct backend execution must stay impossible even before the supervisor exists |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Elevation | HIGH - blocking | backend execution path | mitigate | RED backend-boundary tests plus DTO-only execution contract |
| T-04-02 | Information Disclosure | HIGH - blocking | result envelope | mitigate | bounded stdout/stderr, approved artifact metadata only, no host/runtime internals |
| T-04-03 | Tampering | HIGH - blocking | Python state persistence | mitigate | explicit ownership, expiry, and no caller-controlled policy fields |
| T-04-SC | Tampering | HIGH - blocking | migration and repository layer | mitigate | reviewed schema, explicit constraints, importable repository interfaces |
</threat_model>

## Artifacts this phase produces

- `PythonExecutionResult`, artifact metadata DTOs, and status enums.
- Persistence contracts for conversation-scoped Python state and approved artifacts.
- RED backend-boundary and result-contract tests.

<verification>
Run the RED contract suite and confirm failures are due to missing implementation rather than missing imports or fixtures.
</verification>

<success_criteria>
- The backend cannot accidentally "just run Python" while Phase 4 is under construction.
- Every later plan implements the same bounded result and artifact contract.
- Persisted Python state and reviewed artifacts are explicit, owned, and expiring from the first migration.
</success_criteria>

<output>
Create `.planning/phases/04-isolated-python-execution/04-01-SUMMARY.md` when done.
</output>
