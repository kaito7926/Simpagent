---
phase: 04-isolated-python-execution
plan: "04"
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - frontend/components/chat/MessageBubble.tsx
  - frontend/components/chat/PythonResultCard.tsx
  - frontend/components/chat/PythonStatusBadge.tsx
  - frontend/components/chat/PythonDetailsToggle.tsx
  - frontend/components/chat/PythonArtifactList.tsx
  - frontend/components/chat/ToolDeniedCard.tsx
  - frontend/components/chat/LimitReachedCard.tsx
  - frontend/lib/chat/tool-results.ts
  - frontend/lib/chat/tool-copy.ts
  - frontend/tests/python-result-card.test.tsx
autonomous: true
requirements:
  - CHAT-12
  - SBOX-07
decisions_covered:
  - D-03
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-18
must_haves:
  truths:
    - "Python responses are visually distinct from direct assistant replies and Search results."
    - "Missing permission, policy error, exact-limit termination, and infrastructure failure each have different UI treatment."
    - "Completed user-code exceptions render through the bounded Python result surface, not the infrastructure-failure presentation."
    - "The interface remains natural-language-first and does not introduce a notebook shell or explicit Python mode toggle."
  artifacts:
    - "Dedicated Python result components and typed frontend result presenter."
    - "Frontend tests for Python card states, artifact rendering, and bounded detail disclosure."
    - "Vietnamese Python-specific copy and safe artifact labeling."
  key_links:
    - "This plan consumes the execution envelope fixed in 04-01 and must not create alternate frontend-only state semantics."
    - "The final wire-up to the backend route created in 04-03 must remain possible without UI redesign."
---

<objective>
Render Python-tool results clearly and safely inside the chat experience.

Purpose: turn the backend execution envelope into a user-facing interface that distinguishes Python from other tool and message types without exposing unsafe internals.
Output: dedicated Python/deny/limit cards, artifact list and detail disclosure components, and frontend tests proving the distinction contract.
</objective>

## Phase Goal

**As an** authorized user, **I want to** run bounded Python through the chat workflow, **so that** I can calculate or transform data without gaining access to the host, application network, or secrets.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-isolated-python-execution/04-CONTEXT.md
@.planning/phases/04-isolated-python-execution/04-UI-SPEC.md
@.planning/phases/04-isolated-python-execution/04-PATTERNS.md
@.planning/phases/04-isolated-python-execution/04-VALIDATION.md
@.planning/phases/04-isolated-python-execution/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED frontend tests for Python result states</name>
  <files>frontend/tests/python-result-card.test.tsx</files>
  <read_first>
    <file>.planning/phases/04-isolated-python-execution/04-UI-SPEC.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-CONTEXT.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-01-SUMMARY.md</file>
  </read_first>
  <action>
Create RED component tests for Python success, denied, limit-reached, policy-error, and infrastructure-failure states. Also cover completed user-code exception results that must stay distinct from infrastructure failures while still using bounded detail disclosure. Cover artifact rendering and the requirement that Python surfaces remain distinct from direct assistant and Search result presentation.
  </action>
  <acceptance_criteria>
    - "Tests assert dedicated Python, denied, and limit-reached visual states rather than generic alerts."
    - "Tests cover a completed user-code exception result that does not render as `infra_failure`."
    - "Artifact rendering covers approved small outputs only."
    - "The RED suite does not require a fully implemented backend route to collect."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend npm run test -- python-result-card</automated>
  </verify>
  <done>Frontend state and rendering requirements are explicit before UI code lands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the dedicated Python tool components</name>
  <files>frontend/components/chat/PythonResultCard.tsx, frontend/components/chat/PythonStatusBadge.tsx, frontend/components/chat/PythonDetailsToggle.tsx, frontend/components/chat/PythonArtifactList.tsx, frontend/components/chat/ToolDeniedCard.tsx, frontend/components/chat/LimitReachedCard.tsx</files>
  <read_first>
    <file>frontend/components/account-access/InlineAlert.tsx</file>
    <file>frontend/components/account-access/StatusBadge.tsx</file>
    <file>.planning/phases/04-isolated-python-execution/04-UI-SPEC.md</file>
  </read_first>
  <action>
Build the dedicated Python result components described in `04-UI-SPEC.md`. Reuse the current semantic component style, preserve the established light design language, and keep raw execution details behind progressive disclosure. Do not introduce notebook chrome, a terminal shell aesthetic, or a separate Python workspace.
  </action>
  <acceptance_criteria>
    - "The Python card shows summary, status, duration, and artifact links by default."
    - "The denied and limit-reached cards are distinct from generic error surfaces."
    - "Completed user-code exceptions stay on the Python card surface and expose traceback details only inside the bounded disclosure region."
    - "Expanded raw details remain bounded and visually secondary."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend npm run typecheck</automated>
  </verify>
  <done>The Python-specific UI surfaces exist and match the approved contract.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the typed Python presenter into the chat renderer</name>
  <files>frontend/components/chat/MessageBubble.tsx, frontend/lib/chat/tool-results.ts, frontend/lib/chat/tool-copy.ts</files>
  <read_first>
    <file>.planning/phases/04-isolated-python-execution/04-UI-SPEC.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-RESEARCH.md</file>
    <file>.planning/phases/04-isolated-python-execution/04-01-SUMMARY.md</file>
  </read_first>
  <action>
Add the typed presenter logic that maps the backend Python execution envelope into dedicated chat-renderer components. Preserve natural-language composition, distinguish Python from Search/direct assistant content, ensure completed user-code exceptions do not reuse `infra_failure` semantics, and ensure blocked-import, missing-permission, and exact-limit copy follow the approved Vietnamese contract.
  </action>
  <acceptance_criteria>
    - "Python results do not render as plain assistant markdown."
    - "The presenter uses one shared typed mapping layer instead of scattering state checks across components."
    - "Completed user-code exceptions render as bounded Python execution results rather than infrastructure failures."
    - "No explicit Python mode toggle or notebook flow appears in the chat UI."
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend npm run test -- python-result-card && docker compose run --rm frontend npm run typecheck</automated>
  </verify>
  <done>The chat UI can render all approved Python states without redesigning the application shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Backend result envelope -> frontend renderer | Untrusted raw runtime detail must not become the primary UI state model |
| Artifact metadata -> user download action | Only approved artifacts may reach the user as links or previews |
| Tool state -> chat timeline | Python must remain distinguishable from direct assistant and Search content |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-12 | Spoofing | HIGH - blocking | chat renderer | mitigate | dedicated Python card and typed presenter, not generic assistant markdown |
| T-04-13 | Information Disclosure | HIGH - blocking | expanded details | mitigate | bounded detail disclosure and no host/runtime internals in UI copy |
| T-04-14 | Tampering | HIGH - blocking | artifact actions | mitigate | approved artifact filter and explicit safe labels |
| T-04-15 | Repudiation | MEDIUM | user-facing state | mitigate | explicit denied/limit/policy/infrastructure states with separate copy |
</threat_model>

## Artifacts this phase produces

- Dedicated Python result, deny, and limit-reached chat components.
- Typed frontend presenter for Python execution envelopes.
- Frontend tests for Python state distinction and safe artifact rendering.

<verification>
Run the Python card tests and typecheck, then inspect the rendered states in a browser once the backend wire-up exists.
</verification>

<success_criteria>
- Users can instantly distinguish Python output from other message types.
- Policy and limit states are understandable without exposing unsafe details.
- Artifact actions stay inside the approved small-output boundary.
</success_criteria>

<output>
Create `.planning/phases/04-isolated-python-execution/04-04-SUMMARY.md` when done.
</output>
