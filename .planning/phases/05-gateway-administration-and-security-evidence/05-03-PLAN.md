---
phase: 05-gateway-administration-and-security-evidence
plan: "03"
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - backend/app/api/routes/auth_oauth.py
  - backend/app/identity/oauth_service.py
  - backend/app/identity/providers/github.py
  - backend/app/db/repositories/accounts.py
  - backend/app/core/config.py
  - backend/app/core/provider_status.py
  - backend/app/schemas/health.py
  - backend/app/api/routes/health.py
  - backend/tests/integration/auth/test_github_oauth.py
  - backend/tests/integration/auth/test_oauth_account_linking.py
  - backend/tests/security/test_secret_leakage.py
  - backend/tests/smoke/test_oauth_github_flow.py
autonomous: true
requirements: [IDEN-03, IDEN-07, IDEN-08]
user_setup:
  - service: github-oauth
    why: "Enable real GitHub OAuth login for the protected session flow"
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub Developer Settings -> OAuth Apps"
      - name: GITHUB_CLIENT_SECRET
        source: "GitHub Developer Settings -> OAuth Apps"
      - name: GITHUB_REDIRECT_URI
        source: "Authorized callback URL configured for the OAuth app"
    dashboard_config:
      - task: "Create or update the GitHub OAuth app and register the backend callback URI"
        location: "GitHub Developer Settings -> OAuth Apps"
must_haves:
  truths:
    - "Per D-04 and IDEN-03, a user can start and finish the GitHub OAuth authorization-code flow through backend-owned redirect/callback routes using the same redirect/state/session protection model as the Google provider slice."
    - "Per D-07 and D-08, only a verified normalized email match can auto-link an existing local account; missing, unverified, or conflicting GitHub identity fails closed."
    - "Per D-06, future GitHub logins resolve by persisted provider subject, not mutable provider email."
    - "Per D-05 and IDEN-03, a successful GitHub login lands in the same memory access-token plus HttpOnly refresh-cookie session model as local login and Google login."
    - "Per D-10, backend readiness exposes GitHub capability truth so the dedicated auth-shell plan can later render the CTA without placeholder behavior."
  artifacts:
    - path: backend/app/identity/providers/github.py
      provides: GitHub OAuth provider adapter with verified email resolution
      exports: [GitHubOAuthProvider, GitHubOAuthRequest, GitHubOAuthIdentity]
    - path: backend/tests/integration/auth/test_oauth_account_linking.py
      provides: fail-closed account linking regression coverage
    - path: backend/tests/integration/auth/test_github_oauth.py
      provides: GitHub OAuth integration coverage
    - path: backend/tests/smoke/test_oauth_github_flow.py
      provides: GitHub OAuth assembled smoke coverage
  key_links:
    - from: backend/app/identity/providers/github.py
      to: backend/app/identity/oauth_service.py
      via: verified email and stable subject completion
      pattern: "email_verified|subject"
    - from: backend/app/db/repositories/accounts.py
      to: backend/app/identity/oauth_service.py
      via: subject and normalized-email account lookup
      pattern: "issuer|subject|email_key"
    - from: backend/app/core/provider_status.py
      to: backend/app/api/routes/health.py
      via: readiness-driven GitHub provider state exported for downstream auth-shell wiring
      pattern: "oauth_github|github"
---

<objective>
Complete the backend OAuth login slice by adding GitHub sign-in and enforcing the fail-closed linking/provisioning rules that protect existing local accounts, while leaving the shared frontend CTA/readiness work to Plan 05-09, per D-06, D-07, D-08, and D-10.

Purpose: Expand the usable backend sign-in surface while preserving the exact backend authority and account-takeover protections required for Phase 5.
Output: GitHub provider adapter, strict account-linking logic, readiness exposure, and automated GitHub/account-linking test coverage.
</objective>

## Phase Goal

**As a** user, operator, or authorized administrator, **I want to** use OAuth sign-in, hardened ingress, and redacted admin evidence through one protected application, **so that** the prototype supports safe access and small-scale deployment without weakening backend authority.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-CONTEXT.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-RESEARCH.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-PATTERNS.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-02-SUMMARY.md
@backend/app/identity/oauth_service.py
@backend/app/identity/providers/google.py
@backend/app/db/repositories/accounts.py
@backend/app/models/account.py
</context>

## Artifacts this phase produces

- GitHub provider symbols: `GitHubOAuthProvider`, `GitHubOAuthRequest`, `GitHubOAuthIdentity`.
- Extended OAuth service paths: `complete_login(provider="github", ...)`, conflict and verification denial branches.
- Repository lookup helpers: `get_user_bundle_by_identity_subject(...)`, `get_identities_by_email_key(...)` or equivalent stable-subject lookup APIs.
- Config fields: `github_client_id`, `github_client_secret`, `github_redirect_uri`.
- Readiness/provider-status fields: `oauth_github` on readiness/provider snapshots.
- New tests: `backend/tests/integration/auth/test_github_oauth.py`, `backend/tests/integration/auth/test_oauth_account_linking.py`, `backend/tests/smoke/test_oauth_github_flow.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for GitHub and fail-closed account linking</name>
  <files>
    backend/tests/integration/auth/test_github_oauth.py
    backend/tests/integration/auth/test_oauth_account_linking.py
    backend/tests/security/test_secret_leakage.py
    backend/tests/smoke/test_oauth_github_flow.py
  </files>
  <read_first>
    - backend/tests/integration/auth/test_login.py
    - backend/tests/security/test_secret_leakage.py
    - backend/tests/smoke/test_admin_flow.py
    - backend/app/identity/oauth_service.py
    - backend/app/models/account.py
    - .planning/phases/05-gateway-administration-and-security-evidence/05-CONTEXT.md
    - .planning/phases/05-gateway-administration-and-security-evidence/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: GitHub callback succeeds only when a verified primary email is available and persists the provider subject for later login resolution.
    - Test 2: Existing local account auto-links only when the verified normalized GitHub email exactly matches that local account per D-07.
    - Test 3: Missing email, unverified email, or conflicting identity creates no link, no new privileged session, and no secret leakage per D-08 and D-21.
  </behavior>
  <action>Create the RED coverage for the second OAuth backend slice before changing production code. `backend/tests/integration/auth/test_github_oauth.py` must cover configured and unconfigured GitHub start/callback behavior. `backend/tests/integration/auth/test_oauth_account_linking.py` must cover safe auto-linking, new-user provisioning, missing email denial, unverified email denial, and conflicting identity denial per D-07 and D-08. Extend `backend/tests/security/test_secret_leakage.py` with OAuth-specific canary assertions so provider tokens, secrets, and raw payloads do not leak in error bodies or evidence snippets per D-21.</action>
  <acceptance_criteria>
    - GitHub integration and linking tests exist and fail before implementation.
    - Linking tests assert no new identity or session is created on ambiguous provider identity.
    - Secret-leakage coverage explicitly checks OAuth failure paths.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend python -m pytest tests/integration/auth/test_github_oauth.py tests/integration/auth/test_oauth_account_linking.py tests/security/test_secret_leakage.py tests/smoke/test_oauth_github_flow.py -q</automated>
  </verify>
  <done>Failing tests define the exact GitHub and account-linking behavior required for IDEN-03, IDEN-07, and IDEN-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GitHub OAuth and fail-closed linking rules</name>
  <files>
    backend/app/api/routes/auth_oauth.py
    backend/app/identity/oauth_service.py
    backend/app/identity/providers/github.py
    backend/app/db/repositories/accounts.py
    backend/app/core/config.py
    backend/app/core/provider_status.py
    backend/app/schemas/health.py
    backend/app/api/routes/health.py
    backend/tests/integration/auth/test_github_oauth.py
    backend/tests/integration/auth/test_oauth_account_linking.py
    backend/tests/security/test_secret_leakage.py
    backend/tests/smoke/test_oauth_github_flow.py
  </files>
  <read_first>
    - backend/app/api/routes/auth_oauth.py
    - backend/app/identity/oauth_service.py
    - backend/app/identity/providers/google.py
    - backend/app/db/repositories/accounts.py
    - backend/app/core/config.py
    - backend/app/core/provider_status.py
    - backend/app/schemas/health.py
    - backend/app/models/account.py
    - .planning/phases/05-gateway-administration-and-security-evidence/05-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: GitHub callback uses the same session issuance model as Google/local login after identity verification passes.
    - Test 2: Existing users are resolved by stored provider subject first, then by verified normalized email only for safe first-link cases.
    - Test 3: Ambiguous or unsafe GitHub identities fail closed with no takeover and no secret-bearing output.
  </behavior>
  <action>Extend `backend/app/api/routes/auth_oauth.py` with GitHub start/callback endpoints parallel to the Google slice, implement `backend/app/identity/providers/github.py` to exchange code and fetch a verified email according to GitHub’s email API behavior, and extend `OAuthService` plus `AccountsRepository` with stable-subject-first account resolution. Add `github_client_id`, `github_client_secret`, and `github_redirect_uri` to config, expose `oauth_github` in readiness, and enforce D-07/D-08 exactly: auto-link only on verified normalized email match, auto-provision only on verified new email, and deny missing, unverified, or conflicting email identity with no session issuance. Reuse the same backend-owned redirect/state/session completion model already established for Google so this plan visibly covers the shared IDEN-03 cross-provider redirect/state/session capability as well. Keep this plan backend-focused; do not modify `frontend/lib/auth-session.ts`, `frontend/lib/readiness.ts`, `AuthCard.tsx`, or `AccountAccessShell.tsx` here because shared provider CTA/readiness wiring moves to Plan 05-09.</action>
  <acceptance_criteria>
    - `backend/app/identity/providers/github.py` exists and resolves a stable GitHub subject plus verified email status before linking.
    - `AccountsRepository` exposes the lookup helpers needed to resolve by `(issuer, subject)` before any email-based link attempt.
    - GitHub callback shares the same refresh cookie/session semantics as the Google and local slices.
    - GitHub integration, linking, secret-leakage, and smoke tests pass.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm backend python -m pytest tests/integration/auth/test_github_oauth.py tests/integration/auth/test_oauth_account_linking.py tests/security/test_secret_leakage.py tests/smoke/test_oauth_github_flow.py -q</automated>
  </verify>
  <done>GitHub OAuth works end to end, lands in the same protected session model required by IDEN-03, and all unsafe linking cases fail closed with no takeover path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> FastAPI GitHub OAuth routes | Untrusted browser traffic begins and completes GitHub OAuth flow. |
| FastAPI -> GitHub token/email endpoints | External provider data determines whether linking/provisioning is safe. |
| provider identity -> local account model | Ambiguous provider identity could otherwise attach to the wrong local account. |
| readiness API -> downstream auth shell | Backend capability truth controls later GitHub CTA rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | `backend/app/identity/oauth_service.py` | mitigate | Resolve existing OAuth users by stored `(issuer, subject)` first and allow auto-link only for verified normalized email match per D-06 and D-07. |
| T-05-07 | Spoofing | `backend/app/identity/providers/github.py` | mitigate | Require a verified GitHub email before linking or provisioning and deny ambiguous identities per D-08. |
| T-05-08 | Information Disclosure | OAuth error paths and evidence | mitigate | Extend canary-secret tests so provider tokens, secrets, and raw payloads are redacted from responses and evidence snippets per D-21. |
| T-05-09 | Repudiation | account linking decisions | mitigate | Keep deterministic deny branches and corresponding tests so unsafe provider identities create no silent or partial links. |
</threat_model>

<verification>
- `docker compose run --rm backend python -m pytest tests/integration/auth/test_github_oauth.py tests/integration/auth/test_oauth_account_linking.py tests/security/test_secret_leakage.py tests/smoke/test_oauth_github_flow.py -q`
</verification>

<success_criteria>
- GitHub OAuth works end to end through backend-owned routes.
- Account linking and provisioning fail closed on unsafe identity states.
- Backend readiness exposes truthful GitHub capability state for the shared auth-shell plan.
</success_criteria>

<output>
Create `.planning/phases/05-gateway-administration-and-security-evidence/05-03-SUMMARY.md` when done
</output>
