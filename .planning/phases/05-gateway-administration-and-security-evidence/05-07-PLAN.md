---
phase: 05-gateway-administration-and-security-evidence
plan: "07"
type: execute
wave: 3
depends_on: ["05-04", "05-06"]
files_modified:
  - frontend/lib/admin-api.ts
  - frontend/components/chat/ChatSidebar.tsx
  - frontend/components/chat/ChatWorkspace.tsx
  - frontend/components/settings/SettingsPage.tsx
  - frontend/components/admin/EvidenceTable.tsx
  - frontend/components/admin/EvidenceDetailDrawer.tsx
  - frontend/components/admin/StatePanel.tsx
  - frontend/tests/admin-evidence.test.tsx
  - backend/tests/smoke/test_admin_flow.py
autonomous: true
requirements: [OBS-05, OBS-06]
must_haves:
  truths:
    - "Per D-15, all six admin surfaces are real product surfaces in the shared shell: metrics, users, security events, tool executions, gateway evidence, and orchestration."
    - "Per D-16 and OBS-05, properly scoped admins can page through bounded evidence on each surface."
    - "Per D-19 and D-20, detail drawers show only sanitized bounded snippets from the backend."
    - "Per OBS-06, ordinary users and under-scoped admins see explicit denied states instead of hidden placeholder navigation."
  artifacts:
    - path: frontend/components/admin/EvidenceTable.tsx
      provides: reusable bounded evidence table/card surface
    - path: frontend/components/admin/EvidenceDetailDrawer.tsx
      provides: sanitized evidence detail drawer
    - path: frontend/components/chat/ChatWorkspace.tsx
      provides: fully wired admin surfaces inside the shared shell
    - path: frontend/tests/admin-evidence.test.tsx
      provides: real admin surface wiring tests
  key_links:
    - from: frontend/lib/admin-api.ts
      to: backend/app/api/routes/admin.py
      via: paged users/security/tool/gateway/metrics/orchestration fetches
      pattern: "/api/admin/"
    - from: frontend/components/chat/ChatWorkspace.tsx
      to: frontend/components/admin/EvidenceDetailDrawer.tsx
      via: selected row detail rendering
      pattern: "detail drawer|Xem chi tiết"
    - from: frontend/components/chat/ChatSidebar.tsx
      to: frontend/components/chat/ChatWorkspace.tsx
      via: all six admin navigation targets
      pattern: "Overview|Users|Security events|Tool executions|Gateway evidence|Orchestration"
---

<objective>
Complete the real admin frontend slice by wiring all six admin surfaces to backend data, bounded paging, and sanitized detail drawers inside the shared shell, per D-15, D-16, D-19, and D-20.

Purpose: Turn the already-built backend/admin contracts into actual product surfaces instead of placeholder panels.
Output: Admin evidence table/drawer components, full workspace wiring for all six surfaces, and frontend/smoke coverage.
</objective>

## Phase Goal

**As a** user, operator, or authorized administrator, **I want to** use OAuth sign-in, hardened ingress, and redacted admin evidence through one protected application, **so that** the prototype supports safe access and small-scale deployment without weakening backend authority.

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-CONTEXT.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-UI-SPEC.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-PATTERNS.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-04-SUMMARY.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-06-SUMMARY.md
@frontend/lib/admin-api.ts
@frontend/components/chat/ChatWorkspace.tsx
@frontend/components/chat/ChatSidebar.tsx
</context>

## Artifacts this phase produces

- Frontend admin API wrappers for users, security events, tool executions, gateway evidence, and metrics paging.
- Shared admin UI components: `EvidenceTable`, `EvidenceDetailDrawer`, `StatePanel`.
- Fully wired `ChatWorkspace` admin views for Overview, Users, Security events, Tool executions, Gateway evidence, and Orchestration.
- Real drawer-opening row actions such as `View details` / `Xem chi tiết` backed by sanitized backend snippets.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED frontend tests for all six admin surfaces and denied states</name>
  <files>
    frontend/tests/admin-evidence.test.tsx
    backend/tests/smoke/test_admin_flow.py
  </files>
  <read_first>
    - frontend/tests/admin-evidence.test.tsx
    - frontend/tests/chat-workspace.test.ts
    - backend/tests/smoke/test_admin_flow.py
    - frontend/components/chat/ChatWorkspace.tsx
    - frontend/components/chat/ChatSidebar.tsx
    - .planning/phases/05-gateway-administration-and-security-evidence/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Admin navigation exposes all six surfaces and each one renders backend-backed data or an explicit safe empty state.
    - Test 2: Detail drawers open from evidence rows and show only sanitized snippet fields returned by the backend.
    - Test 3: Ordinary users and under-scoped admins receive explicit forbidden-state UI instead of hidden routes or fake data.
  </behavior>
  <action>Write the frontend/admin RED tests before wiring the remaining views. Expand `frontend/tests/admin-evidence.test.tsx` so it covers sidebar navigation, users/events/tools/gateway paging shells, detail drawers, empty states, and explicit forbidden states per D-16 and the `05-UI-SPEC.md` admin evidence contract. Extend `backend/tests/smoke/test_admin_flow.py` only as needed to support end-to-end assertions that the newly wired frontend/admin route contract is backed by real backend data.</action>
  <acceptance_criteria>
    - RED tests fail until all six surfaces are wired.
    - Drawer tests assert sanitized snippets only, not raw metadata dumps.
    - Forbidden-state rendering is explicitly tested for non-admin or under-scoped views.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- frontend/tests/admin-evidence.test.tsx && docker compose run --rm backend python -m pytest tests/smoke/test_admin_flow.py -q</automated>
  </verify>
  <done>Failing tests define the full admin frontend contract for all six Phase 5 surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reusable evidence table and drawer components from the UI contract</name>
  <files>
    frontend/components/admin/EvidenceTable.tsx
    frontend/components/admin/EvidenceDetailDrawer.tsx
    frontend/components/admin/StatePanel.tsx
    frontend/tests/admin-evidence.test.tsx
  </files>
  <read_first>
    - frontend/components/ui/table.tsx
    - frontend/components/ui/sheet.tsx
    - frontend/components/ui/skeleton.tsx
    - frontend/components/chat/ChatWorkspace.tsx
    - .planning/phases/05-gateway-administration-and-security-evidence/05-UI-SPEC.md
  </read_first>
  <action>Create semantic admin components that match the locked Phase 5 UI contract: `EvidenceTable` for bounded desktop/mobile evidence lists, `EvidenceDetailDrawer` for redacted detail inspection, and `StatePanel` for loading/empty/forbidden/error states. Keep drawer content bounded and driven only by backend-provided sanitized snippets per D-19 and D-20; do not add raw JSON viewers, debug chrome, or fake placeholders forbidden by D-28.</action>
  <acceptance_criteria>
    - New admin components exist under `frontend/components/admin/` and match the shared shell’s shadcn-based UI language.
    - Drawer content is bounded and does not render raw nested payload dumps.
    - Frontend tests pass for component-level table/drawer behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- frontend/tests/admin-evidence.test.tsx</automated>
  </verify>
  <done>Reusable admin evidence UI primitives exist and enforce bounded, sanitized display behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all six admin surfaces into the shared shell with real backend paging</name>
  <files>
    frontend/lib/admin-api.ts
    frontend/components/chat/ChatSidebar.tsx
    frontend/components/chat/ChatWorkspace.tsx
    frontend/components/settings/SettingsPage.tsx
    frontend/tests/admin-evidence.test.tsx
    backend/tests/smoke/test_admin_flow.py
  </files>
  <read_first>
    - frontend/lib/admin-api.ts
    - frontend/components/chat/ChatSidebar.tsx
    - frontend/components/chat/ChatWorkspace.tsx
    - frontend/components/settings/SettingsPage.tsx
    - frontend/components/admin/EvidenceTable.tsx
    - frontend/components/admin/EvidenceDetailDrawer.tsx
    - .planning/phases/05-gateway-administration-and-security-evidence/05-UI-SPEC.md
    - .planning/phases/05-gateway-administration-and-security-evidence/05-06-SUMMARY.md
  </read_first>
  <action>Extend `frontend/lib/admin-api.ts` with paged wrappers for users, security events, tool executions, gateway evidence, and metrics. Update `ChatSidebar.tsx` so all six admin labels are first-class navigation items per D-15. In `ChatWorkspace.tsx`, replace placeholder admin panels with real paged views that use `EvidenceTable`, `EvidenceDetailDrawer`, and `StatePanel` for Users, Security events, Tool executions, and Gateway evidence; keep Overview and Orchestration integrated from Plan 04. Preserve explicit forbidden states for under-scoped users per D-16 and OBS-06, and keep `SettingsPage.tsx` aligned with the shared shell instead of creating a separate admin app.</action>
  <acceptance_criteria>
    - Sidebar lists all six admin destinations from the shared shell.
    - Each admin surface loads backend data through `authorizedJson` wrappers and supports bounded paging or explicit empty states.
    - Detail drawers display sanitized snippets only.
    - Frontend/admin smoke tests pass.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- frontend/tests/admin-evidence.test.tsx frontend/tests/chat-workspace.test.ts && npm --prefix frontend typecheck && docker compose run --rm backend python -m pytest tests/smoke/test_admin_flow.py -q</automated>
  </verify>
  <done>All six admin surfaces are real, navigable product pages backed by bounded backend data inside the shared shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> admin API wrappers | Untrusted client UI requests sensitive admin evidence through protected endpoints. |
| backend evidence payload -> drawer UI | Sanitized snippets cross from backend contracts into browser detail views. |
| admin navigation -> protected surfaces | UI must not imply access when backend scopes deny it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Information Disclosure | `frontend/components/admin/EvidenceDetailDrawer.tsx` | mitigate | Render only backend-sanitized snippet fields and bounded detail sections; do not add raw JSON or payload dumps per D-20. |
| T-05-23 | Elevation of Privilege | shared admin shell | mitigate | Show explicit forbidden states for under-scoped users, but keep all meaningful authorization in backend API calls per OBS-06. |
| T-05-24 | Spoofing | admin navigation affordances | mitigate | Use backend-provided data and scopes for page content; never fabricate admin evidence or pretend a denied user has access. |
| T-05-25 | Repudiation | admin detail inspection | mitigate | Preserve correlation IDs and row-level references in evidence tables/drawers so viewed evidence remains traceable. |
</threat_model>

<verification>
- `npm --prefix frontend test -- frontend/tests/admin-evidence.test.tsx frontend/tests/chat-workspace.test.ts`
- `npm --prefix frontend typecheck`
- `docker compose run --rm backend python -m pytest tests/smoke/test_admin_flow.py -q`
</verification>

<success_criteria>
- All six admin surfaces are real and navigable from the shared shell.
- Paged evidence and detail drawers use sanitized backend data only.
- Under-scoped users see explicit denied states instead of hidden placeholder behavior.
</success_criteria>

<output>
Create `.planning/phases/05-gateway-administration-and-security-evidence/05-07-SUMMARY.md` when done
</output>
