---
phase: 03-policy-controlled-google-search
plan: "08"
type: execute
wave: 7
depends_on:
  - 03-06
  - 03-07
files_modified:
  - backend/tests/smoke/test_google_search_flow.py
  - backend/tests/smoke/test_admin_flow.py
  - .planning/phases/03-policy-controlled-google-search/03-VALIDATION.md
autonomous: true
requirements:
  - AUTHZ-04
  - AGNT-06
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
must_haves:
  truths:
    - "Gemini default, Firecrawl override, Firecrawl-unconfigured, and override-clear flows all have assembled smoke coverage instead of backend-only proof."
    - "The validation contract explicitly names the dual-provider matrix and keeps SRCH-05 retention/no-click-tracking coverage in the required automated commands."
    - "Admin provider override flows and provider-honest search rendering remain part of the final closeout evidence rather than side notes."
  artifacts:
    - "`backend/tests/smoke/test_google_search_flow.py` covers the Gemini and Firecrawl public search matrix plus the fail-closed Firecrawl-unconfigured case."
    - "`backend/tests/smoke/test_admin_flow.py` covers provider override set and clear behavior through the assembled admin surface."
    - "`.planning/phases/03-policy-controlled-google-search/03-VALIDATION.md` is updated so the quick/full/smoke commands explicitly cover the enhancement matrix and the retention-allowlist suite."
  key_links:
    - "Smoke tests and validation commands point at the same provider matrix so execution evidence cannot drift from the written validation contract."
    - "The final validation commands continue to include `test_search_retention_allowlist.py` so SRCH-05 stays explicit in closeout evidence."
---

<objective>
Close the enhancement pack with assembled smoke coverage and validation updates for the Gemini and Firecrawl provider matrix.

Purpose: keep the final enhancement proof focused and under budget by separating smoke and validation closeout from backend runtime, admin persistence, and frontend UX work.
Output: dual-provider smoke tests, admin override smoke coverage, and an updated `03-VALIDATION.md` that explicitly includes Firecrawl and SRCH-05 retention/no-click-tracking verification.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-policy-controlled-google-search/03-CONTEXT.md
@.planning/phases/03-policy-controlled-google-search/03-VALIDATION.md
@.planning/phases/03-policy-controlled-google-search/03-05-PLAN.md
@.planning/phases/03-policy-controlled-google-search/03-06-PLAN.md
@.planning/phases/03-policy-controlled-google-search/03-07-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED assembled smoke expectations for the dual-provider matrix</name>
  <files>backend/tests/smoke/test_google_search_flow.py, backend/tests/smoke/test_admin_flow.py</files>
  <behavior>
    - Test 1: assembled smoke covers Gemini default success, Firecrawl override success, and Firecrawl-selected-but-unconfigured `search_unavailable` behavior.
    - Test 2: assembled admin smoke covers provider override set and clear actions through the real admin surface.
  </behavior>
  <action>Extend the smoke suites before implementation so the enhancement closes with assembled-topology evidence rather than only narrow tests. Add RED cases to `backend/tests/smoke/test_google_search_flow.py` and `backend/tests/smoke/test_admin_flow.py` that exercise the provider matrix through public-stack entry points: Gemini as default, Firecrawl via admin override, Firecrawl-unconfigured fail closed per D-11, and override-clear back to the environment default per D-12. Keep the smoke assertions focused on externally visible state and correlated evidence, not internal helper shortcuts.</action>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/smoke/test_google_search_flow.py tests/smoke/test_admin_flow.py -x</automated>
  </verify>
  <done>The dual-provider assembled behavior is captured in failing smoke tests before the closeout changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Update smoke suites and validation commands for provider-matrix closeout</name>
  <files>backend/tests/smoke/test_google_search_flow.py, backend/tests/smoke/test_admin_flow.py, .planning/phases/03-policy-controlled-google-search/03-VALIDATION.md</files>
  <action>Implement the closeout matrix and update the written validation contract to match it. Make the smoke tests pass for Gemini default success, Firecrawl override success, Firecrawl-unconfigured `search_unavailable`, and provider-override clear behavior through the assembled admin surface. Then revise `03-VALIDATION.md` so its quick/full/smoke commands explicitly mention the enhanced provider matrix and continue to include `backend/tests/security/test_search_retention_allowlist.py` for SRCH-05 retention and no-click-tracking verification. Do not collapse the closeout language back to a generic "search works" claim.</action>
  <verify>
    <automated>docker compose -f compose.test.yaml run --rm backend-test pytest -q tests/integration/search/test_search_capability_check.py tests/integration/search/test_search_persistence.py tests/security/test_search_retention_allowlist.py tests/integration/admin/test_admin_write.py tests/smoke/test_google_search_flow.py tests/smoke/test_admin_flow.py -x && cd frontend && npm test -- tests/search-session.test.ts tests/search-rendering.test.tsx tests/admin-evidence.test.tsx && npm run typecheck</automated>
  </verify>
  <done>The enhancement pack ends with explicit Gemini/Firecrawl smoke evidence and a validation file that names the final regression matrix, including SRCH-05 coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validation contract -> executed smoke matrix | Written verification claims must match the real automated provider matrix |
| Public search/admin entry points -> smoke assertions | End-to-end evidence must prove external behavior, not only internal unit seams |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-31 | Repudiation | validation closeout | mitigate | Update `03-VALIDATION.md` so all final commands explicitly cover the Gemini/Firecrawl matrix and SRCH-05 retention suite |
| T-03-32 | Tampering | smoke evidence scope | mitigate | Keep separate smoke cases for Gemini default, Firecrawl override, Firecrawl-unconfigured, and override-clear flows |
| T-03-33 | Information Disclosure | retention verification | mitigate | Preserve `test_search_retention_allowlist.py` in final automated commands so no-click-tracking and allowlisted persistence remain part of closeout proof |
</threat_model>

<verification>
Run the Task 2 command, then confirm `03-VALIDATION.md` and the smoke files refer to the same Gemini/Firecrawl matrix and still include the retention-allowlist suite for SRCH-05.
</verification>

<success_criteria>
- The enhancement pack ends with explicit smoke proof for Gemini, Firecrawl, and Firecrawl-unconfigured behavior.
- Validation commands stay synchronized with the real provider matrix.
- SRCH-05 retention/no-click-tracking coverage remains visible in final closeout artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-policy-controlled-google-search/03-08-SUMMARY.md` when done
</output>
