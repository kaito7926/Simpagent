---
phase: 03-policy-controlled-google-search
plan: "07"
type: execute
wave: 6
depends_on:
  - 03-06
  - 03-03
files_modified:
  - frontend/lib/admin-api.ts
  - frontend/lib/chat-session.ts
  - frontend/components/settings/SettingsPage.tsx
  - frontend/components/chat/ChatWorkspace.tsx
  - frontend/components/chat/ConversationHeader.tsx
  - frontend/components/chat/ToolModeSwitch.tsx
  - frontend/components/chat/GroundedAnswer.tsx
  - frontend/components/chat/SearchFailureCard.tsx
  - frontend/tests/search-session.test.ts
  - frontend/tests/search-rendering.test.tsx
  - frontend/tests/admin-evidence.test.tsx
autonomous: true
requirements:
  - SRCH-03
  - SRCH-04
  - SRCH-06
  - SRCH-07
must_haves:
  truths:
    - "D-13: user-visible badges, labels, helper text, and failure copy stay provider-honest; Google-only language never appears when Firecrawl is active."
    - "D-12: the admin settings surface can show default, override, effective provider, and readiness without exposing secrets."
    - "Grounded, missing-grounding, denied, unavailable, provider-failed, and timeout states remain visually distinct after provider generalization."
    - "Vietnamese copy expectations remain preserved where Phase 3 already established them; only provider-specific wording changes when honesty requires it."
    - "Suggestion, citation, and source-list behavior from D-01 through D-09 still works for Gemini while Firecrawl omits Google-specific affordances it does not provide."
  artifacts:
    - "`frontend/lib/admin-api.ts` and `frontend/lib/chat-session.ts` expose provider-aware contracts for admin settings and chat rendering."
    - "`frontend/components/settings/SettingsPage.tsx` renders admin provider default/override/effective-provider controls using the existing orchestration interaction style."
    - "`frontend/components/chat/GroundedAnswer.tsx`, `SearchFailureCard.tsx`, `ToolModeSwitch.tsx`, `ConversationHeader.tsx`, and `ChatWorkspace.tsx` render provider-honest labels and states."
    - "`frontend/tests/search-session.test.ts`, `frontend/tests/search-rendering.test.tsx`, and `frontend/tests/admin-evidence.test.tsx` prove the provider-honest UI contract before and after implementation."
  key_links:
    - "`frontend/lib/admin-api.ts` -> `frontend/components/settings/SettingsPage.tsx` consumes the D-12 admin orchestration contract from 03-06 directly."
    - "`frontend/lib/chat-session.ts` -> `frontend/components/chat/*` maps trusted backend provider metadata into badges, labels, suggestions, and failure copy."
    - "Frontend tests lock provider-honest behavior so Firecrawl cannot regress into Google-only UI copy later."
---

<objective>
Generalize the chat and admin frontend from Google-only assumptions to provider-honest Gemini and Firecrawl behavior while preserving the existing Vietnamese UX contract.

Purpose: isolate the provider-aware UI and admin contract work from backend runtime and smoke/validation closeout so each plan stays under the file-budget threshold and D-13 is explicit.
Output: provider-aware frontend types, provider-honest chat/admin rendering, and frontend regression tests for search session, rendering, and admin evidence behavior.
</objective>

<execution_context>
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/workflows/execute-plan.md
@D:/ADMIN/Documents/matmahoc/@DO_AN/Simpagent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-policy-controlled-google-search/03-CONTEXT.md
@.planning/phases/03-policy-controlled-google-search/03-RESEARCH.md
@.planning/phases/03-policy-controlled-google-search/03-PATTERNS.md
@.planning/phases/03-policy-controlled-google-search/03-UI-SPEC.md
@.planning/phases/03-policy-controlled-google-search/03-03-SUMMARY.md
@.planning/phases/03-policy-controlled-google-search/03-06-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock provider-honest chat and admin UX expectations in RED frontend tests</name>
  <files>frontend/tests/search-session.test.ts, frontend/tests/search-rendering.test.tsx, frontend/tests/admin-evidence.test.tsx</files>
  <behavior>
    - Test 1: per D-13, Gemini-grounded responses retain `Google-grounded` while Firecrawl responses never show Google-only badges or wording.
    - Test 2: missing-grounding, denied, unavailable, provider-failed, and timeout states remain visually distinct with the required Vietnamese copy expectations.
    - Test 3: the settings/admin view shows provider default, override, effective provider, and clear action without exposing secrets.
  </behavior>
  <action>Rewrite the frontend expectations before implementation so D-13 is executable. Add RED assertions that Gemini keeps the existing `Google-grounded` and Google-search mode copy where required, Firecrawl grounded results use provider-honest labels instead of Google-only language, failure cards mention either the active provider or neutral `dịch vụ tìm kiếm` wording as appropriate, and the settings surface renders default, override, effective provider, readiness, and clear-override flows without exposing any credential material. Preserve the current Vietnamese copy expectations from Phase 3 wherever provider honesty does not require a change.</action>
  <verify>
    <automated>cd frontend && npm test -- tests/search-session.test.ts tests/search-rendering.test.tsx tests/admin-evidence.test.tsx</automated>
  </verify>
  <done>The provider-honest UI contract is encoded in failing frontend tests before any implementation files are changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider-aware frontend contracts and provider-honest rendering</name>
  <files>frontend/lib/admin-api.ts, frontend/lib/chat-session.ts, frontend/components/settings/SettingsPage.tsx, frontend/components/chat/ChatWorkspace.tsx, frontend/components/chat/ConversationHeader.tsx, frontend/components/chat/ToolModeSwitch.tsx, frontend/components/chat/GroundedAnswer.tsx, frontend/components/chat/SearchFailureCard.tsx</files>
  <behavior>
    - Test 1: chat-session transport and view-model types carry provider metadata needed for honest rendering.
    - Test 2: Gemini grounded responses preserve the Phase 3 Google-specific contract, while Firecrawl grounded responses use provider-specific or neutral labels without false Google claims.
    - Test 3: admin settings can view, set intent for, and clear the provider override through the D-12 contract from 03-06.
  </behavior>
  <action>Implement D-12 and D-13 end to end in the frontend. Extend `frontend/lib/admin-api.ts` and `frontend/lib/chat-session.ts` with provider fields from the backend orchestration and search contracts, then update `SettingsPage.tsx`, `ChatWorkspace.tsx`, `ConversationHeader.tsx`, `ToolModeSwitch.tsx`, `GroundedAnswer.tsx`, and `SearchFailureCard.tsx` so provider-neutral behavior is the default and provider-specific labels are selected only from trusted backend metadata. Preserve Vietnamese copy from the existing Phase 3 UI where possible, keep `Google-grounded` only for true Gemini-grounded turns per D-13, omit Google-only affordances on Firecrawl turns, and keep suggestion, citation, and retry behavior aligned with the existing Phase 3 security/UX contract. Reuse the current orchestration confirm/disable interaction style in settings rather than inventing a new admin-control pattern.</action>
  <verify>
    <automated>cd frontend && npm test -- tests/search-session.test.ts tests/search-rendering.test.tsx tests/admin-evidence.test.tsx && npm run typecheck</automated>
  </verify>
  <done>The frontend truthfully presents Gemini and Firecrawl behavior without regressing the Phase 3 state matrix or Vietnamese UX contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Backend provider metadata -> frontend labels and badges | Runtime provider state must be rendered honestly without overclaiming Google grounding |
| Admin orchestration response -> settings controls | Privileged provider settings must stay secret-free and RBAC-consistent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-27 | Information Disclosure | settings/admin provider contract | mitigate | Render only default/override/effective-provider/readiness fields from the backend contract and never expose secrets |
| T-03-28 | Tampering | chat provider-label mapping | mitigate | Map provider metadata through typed `chat-session.ts` contracts and lock D-13 with frontend tests |
| T-03-29 | Spoofing | grounded badge rendering | mitigate | Reserve `Google-grounded` for actual Gemini grounded turns and use provider-honest labels for Firecrawl |
| T-03-30 | Repudiation | provider-honest UX regressions | mitigate | Keep provider-honest session/rendering/admin tests as automated gates for the frontend contract |
</threat_model>

<verification>
Run the Task 2 command, then confirm the frontend test suite proves: Gemini grounded output retains Google-specific evidence UI, Firecrawl grounded output avoids Google-only labels, and the settings page can represent provider default, override, effective provider, and clear action.
</verification>

<success_criteria>
- The frontend never mislabels Firecrawl results as Google-grounded.
- Admin users can understand the provider override state without seeing secret-bearing details.
- Existing Vietnamese chat/search copy remains intact except where provider honesty requires a provider-specific or neutral replacement.
</success_criteria>

<output>
Create `.planning/phases/03-policy-controlled-google-search/03-07-SUMMARY.md` when done
</output>
