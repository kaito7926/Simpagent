---
phase: 06-adversarial-verification-and-vietnamese-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/auth/**
  - backend/tests/integration/chat/**
  - backend/tests/integration/search/**
  - backend/tests/integration/python/**
  - backend/tests/security/**
  - backend/tests/smoke/**
  - security-tests/run-phase6-matrix.ps1
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-09, TEST-10]
---

<objective>
Build the repeatable automated evidence matrix for Phase 6 by mapping existing auth/chat/search/python/gateway/admin tests to the final requirements, filling the real coverage gaps, and adding one repo-owned runner that emits a trustworthy pass/fail summary.
</objective>

## Scope

- Consolidate existing automated coverage for auth lifecycle, BOLA, admin/tool authorization, chat behavior, search safety, Python isolation, side-effect assertions, and canary-secret scans.
- Add only the missing tests or helpers required to make the final proof claims truthful.
- Create a single phase-level runner under `security-tests/` so evaluators do not need to guess which subsets to run.

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-adversarial-verification-and-vietnamese-delivery/06-CONTEXT.md
@.planning/phases/05-gateway-administration-and-security-evidence/05-VERIFICATION.md
@backend/tests/integration/
@backend/tests/security/
@backend/tests/smoke/
@frontend/tests/
</context>

## Tasks

1. Inventory the current test corpus against `TEST-01` through `TEST-06`, `TEST-09`, and `TEST-10`, then record the exact remaining gaps.
2. Add or tighten the missing tests so the final matrix proves side effects and non-leakage instead of response wording only.
3. Create `security-tests/run-phase6-matrix.ps1` to run the agreed backend/frontend subsets and print a bounded phase evidence summary.

<verification>
- `docker compose run --rm backend python -m pytest tests/integration tests/security tests/smoke -q`
- `docker compose run --rm frontend npm test --`
- `powershell -ExecutionPolicy Bypass -File security-tests/run-phase6-matrix.ps1`
</verification>

<success_criteria>
- Each final verification requirement from `TEST-01` to `TEST-06`, plus `TEST-09` and `TEST-10`, maps to at least one repeatable automated command.
- Missing coverage is closed with code, not with wording in the report.
- The matrix runner exits nonzero on failure and can be rerun from the repo root on Windows.
</success_criteria>

<output>
Create `.planning/phases/06-adversarial-verification-and-vietnamese-delivery/06-01-SUMMARY.md` when done.
</output>
