---
phase: 06-adversarial-verification-and-vietnamese-delivery
plan: "02"
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - security-tests/README.md
  - security-tests/run-phase6-attacks.ps1
  - security-tests/**
requirements: [TEST-07, TEST-09]
---

<objective>
Create the black-box adversarial suite under `security-tests/` so evaluators can run replay, BOLA, brute-force, SSRF, prompt/tool abuse, and sandbox-abuse attempts against the owned Compose stack and observe denied behavior with no forbidden side effects.
</objective>

## Scope

- Use focused attack scripts plus shared helpers rather than one monolithic script.
- Keep all attack targets inside the owned local application.
- Record evidence as bounded exit codes, summary files, and safe console output.

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@compose.yaml
@backend/tests/smoke/
@backend/tests/security/
</context>

## Tasks

1. Build shared attack helpers for login/session bootstrap, HTTP requests, bounded DB/evidence checks, and result formatting.
2. Add focused attack scripts for brute force/rate limit, refresh replay, two-user BOLA, SSRF or internal reachability, prompt/tool abuse, and sandbox escape/resource abuse.
3. Add `security-tests/run-phase6-attacks.ps1` plus a README that explains prerequisites, safe scope, and expected outcomes.

<verification>
- `docker compose up --build --wait`
- `powershell -ExecutionPolicy Bypass -File security-tests/run-phase6-attacks.ps1`
</verification>

<success_criteria>
- Attack scripts fail the run on any forbidden side effect or unexpected allow-path.
- The suite is limited to the owned local stack and does not attempt third-party targets.
- The README is sufficient for an evaluator to run the attacks without reading internal planning files first.
</success_criteria>

<output>
Create `.planning/phases/06-adversarial-verification-and-vietnamese-delivery/06-02-SUMMARY.md` when done.
</output>
