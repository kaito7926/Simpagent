---
quick_id: 260624-3t5
type: quick
status: planned
created: 2026-06-23T19:44:35Z
---

# Quick Task 260624-3t5: Fix Phase 03 Firecrawl websearch UAT UI issue report

## Objective

Fix the Firecrawl websearch UAT issue documented in `firecrawl-websearch-uat-ui.md`: raw `[1]` result markers should become structured clickable citations, source links should read as links, and the grounded badge must not display the misleading text `Firecrawl-grounded`.

## Tasks

1. Update the Firecrawl normalization contract so sourced Firecrawl results produce shaped answer text with structured citation positions instead of raw `[n]` lines.
2. Update the grounded answer UI and tests so Firecrawl uses neutral sourced-web copy, keeps Google-only suggestions hidden, and shows source links as obvious links.
3. Run focused backend/frontend tests and update the debug report resolution.

## Verification

- `python -m pytest backend/tests/security/test_search_retention_allowlist.py -q`
- `npm --prefix frontend test -- tests/search-rendering.test.tsx`
