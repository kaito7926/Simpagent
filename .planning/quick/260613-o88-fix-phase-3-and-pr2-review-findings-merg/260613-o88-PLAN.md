# Quick Task 260613-o88: Fix Phase 3 Integration and Add Agent Orchestration

**Date:** 2026-06-13
**Status:** Completed

## Goal

Merge `origin/phase-3` onto the current `origin/main` line, keep phase 04 sandbox work intact, fix review findings, and add a Google ADK-style orchestration layer:

User -> Coordinator Agent
- WebSearchAgent
- CodeSandboxAgent
- Loop Agent
- Guardrail / Safety Check Agent
- ReportWriterAgent

Admin users must be able to toggle the guardrail/safety check agent.

## Tasks

1. [x] Merge `origin/phase-3` into the integration branch and resolve conflicts without dropping phase 02/04 behavior.
2. [x] Fix review findings:
   - Python runtime exceptions must not be reported as `succeeded`.
   - Sandbox policy must block dangerous `os` process-control APIs.
   - Runtime security reporting must be honest about default seccomp unless a real profile is applied.
   - Phase 3 Compose/search defaults must not silently leave Google Search unavailable.
3. [x] Add orchestration code and contract tests for Coordinator, WebSearchAgent, CodeSandboxAgent, LoopAgent, GuardrailSafetyAgent, and ReportWriterAgent.
4. [x] Add admin-facing backend controls for enabling/disabling the guardrail safety agent.
5. [x] Run targeted backend/frontend tests and record verification.

## Verification

- `python -m compileall backend/app backend/tests/unit/test_admin_evidence_service.py backend/tests/integration/chat/test_message_send.py backend/tests/security/test_search_guardrails.py sandbox/server.py sandbox/runtime/runner.py sandbox/runtime/sitecustomize.py`
- `.venv\Scripts\python.exe -m pytest backend/tests/unit/python/test_result_envelope.py backend/tests/security/test_python_policy_denials.py backend/tests/security/test_python_runtime_profile.py backend/tests/unit/test_admin_evidence_service.py -q`
- `docker compose -f compose.test.yaml run --build --rm backend-test python -m pytest -m "not smoke" -q`
- `npm run typecheck`
- `npm test`
- `npm audit --omit=dev && npm audit`
