---
status: complete
date: 2026-06-22
commit: uncommitted
---

# Quick Task 260622-m55 Summary

## Completed

- Added a provisioned Grafana dashboard, `SimpAgent Request Journey`, under `observability/grafana/provisioning/dashboards/`.
- Designed the dashboard around manual `correlation_id` and `path` textbox filters so one request can be narrowed without turning high-cardinality fields into Loki labels.
- Added request-entry, correlated tool/security-event, status, duration, and correlation-id panels that reuse the existing Loki and Tempo datasource setup.
- Extended the observability provisioning test suite to validate the new dashboard file, fixed `uid/title`, and correlation-aware Loki queries.
- Updated the observability README section so operators know the new dashboard is available beside the overview dashboard.

## Verification

- `python -m pytest -q backend/tests/integration/gateway/test_observability_tracing_profile.py` passed (`7 passed`).
- `docker compose exec -T grafana sh -lc "ls -1 /etc/grafana/provisioning/dashboards"` showed `simpagent-request-journey.json` mounted in the Grafana container.
- `docker compose restart grafana` completed successfully.
- `docker compose logs grafana --since 20s` showed `starting to provision dashboards` followed by `finished to provision dashboards` after the restart.

## Notes

- The dashboard deliberately keeps `correlation_id` as a parsed JSON field and filters it inside LogQL, which is safer for Loki cardinality than promoting it to a log label.
- Grafana HTTP search could not be used for verification because the running persisted Grafana volume currently rejects `admin/admin`; provisioning was verified through mounted files and startup logs instead.
