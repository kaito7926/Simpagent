---
status: in_progress
date: 2026-06-21
---

# Quick Task 260621-l7m: Stabilize message-at-rest encryption rollout

## Must Haves

- Keep the chosen scope explicit: session-only auth cookies plus application-layer encryption at rest for message content, not true E2EE.
- Update migration coverage so Alembic head and message-content encryption backfill match revision `0005_encrypt_message_content`.
- Re-run the targeted tests in the Compose-backed test topology where PostgreSQL is reachable.

## Tasks

1. Fix stale migration assertions and add a data-migration regression test for plaintext-to-ciphertext backfill.
2. Run the targeted auth/encryption/migration tests, including the DB-backed cases in `compose.test.yaml`.
3. Record results in the quick-task summary and project state.
