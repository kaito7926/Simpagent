---
status: in_progress
quick_task: 260611-rv2
description: improve admin RBAC and evidence endpoints
created: 2026-06-11
---

## Objective

Add explicit business-layer admin RBAC enforcement and minimal admin evidence endpoints without expanding into unrelated Phase 5 gateway or observability work.

## Tasks

1. Add explicit admin role-and-scope policy checks and denial handling.
2. Add read-only admin endpoints for bounded users, security events, tool executions, and aggregate metrics using existing persistence tables.
3. Add tests for admin allow/deny paths and service shaping that can run without the PostgreSQL Compose test host.

## Verification

- `cd backend && python -m pytest tests/unit/test_admin_policy.py tests/unit/test_admin_evidence_service.py -q --tb=short`
- `cd backend && python -m compileall app tests`
