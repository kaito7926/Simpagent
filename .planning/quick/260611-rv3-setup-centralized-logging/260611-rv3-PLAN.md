---
status: in_progress
quick_task: 260611-rv3
description: setup centralized logging with Grafana Loki and JSON logs
created: 2026-06-11
---

## Objective

Add a local centralized logging stack that can monitor application access and security-relevant events without leaking secrets.

## Tasks

1. Add structured JSON logging in the FastAPI backend with correlation-aware access logging.
2. Add Loki, Promtail, and Grafana services to the local Compose topology and provision Grafana with a Loki datasource.
3. Route backend and gateway logs into shared log volumes that Promtail can scrape safely on Docker Desktop.
4. Document the local observability entrypoints and the expected log streams.

## Verification

- `docker compose config -q`
- `cd backend && python -m pytest tests/integration/auth tests/security/test_jwt_profile.py tests/security/test_principal_fail_closed.py tests/security/test_unknown_policy_state.py -q --tb=short`
